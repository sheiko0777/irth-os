node_modules
.pnpm-store
dist
build
.next
.turbo
.vercel
.cache
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# TypeScript incremental build info
*.tsbuildinfo

# Analysis artifacts (graphify / CodeFlow) and stale nested clone
graphify-out/
codeflow-*
hazy-spinning-blum.md
/irth-os/

# Wrangler local secrets — never commit
.dev.vars
.dev.vars.*

# Wrangler local state
.wrangler/

# Scratch/worktree dir created by tooling; contains nested git repos.
.freebuff/
