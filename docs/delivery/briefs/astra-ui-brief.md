# IRTH OS admin — UI architecture, design system, and mobile-first screen program (design-direction brief)

You are the design lead + frontend architect for a rebuild of the admin/control-room UI of IRTH OS. Produce ONE decisive markdown spec (target 3,000–4,500 words plus tables). Read-only sandbox: inspect the repo, read official docs, do NOT write files. Be opinionated; say what you would refuse.

## Product in one paragraph
IRTH OS = one practical control room for an Egyptian consumer-goods group: parent brand IRTH + 3 legally separate brands, ONE operations team. Channels: own storefronts (Shopify + WooCommerce, 3+ domains), WhatsApp/Instagram DM manual orders, a physical shop (web POS, online-only), wholesale B2B, marketplaces later. Money: COD via couriers (dominant), gateways, bank/InstaPay, B2B credit. Inventory: buy-and-resell + in-house manufacturing/packaging with lots/expiry (cosmetics). Full statutory accounting per legal entity + consolidated group. Users: owner/admin (phone-heavy, wants to "never open the Shopify admin again"), accountant (desktop, dense), ops/warehouse/shipping staff (phone/tablet on the floor), POS cashier (tablet), external suppliers (portal: see POs, confirm deliveries). Arabic-first, RTL, with English. Cheapest possible; no paid design/UI SaaS; solo owner directing coding agents (Codex/Jules/Hermes/Claude) in parallel — so the spec must decompose into independent build packets.

## The architecture decision already made (do not re-litigate)
A separate astra review concluded: modular monolith; keep the proven kernel (double-entry ledger, WAC costing, RLS tenancy, outbox/inbox); unit of delivery = one COMPLETE business transaction, starting with the owner's real Shopify order that today shows only "order number + price" (no items, no customer, no address — a facade). Storefront-admin parity contract: for every order the admin shows everything needed to identify the buyer, understand every item, fulfil, explain the money, handle a return, inspect the source; each section carries a status: `loaded` / `not_applicable` / `not_exposed_by_provider` / `permission_denied` / `fetch_failed`; failed imports appear as "Import blocked" with the exact missing section and next action, never as a half-order. Five approval policies + one approvals table. Fixed role templates. Hand-written SQL reports (8): sales/contribution by entity·brand·channel·currency; orders needing attention (incl. blocked imports); stock availability/ageing/expiry/stockouts; funnel by storefront/device/campaign; repeat/cohorts; courier delivery/RTO/COD remittance; fulfilment throughput/turnaround/errors; site speed + integration health. Analytics: first-party pixel → Worker `/collect` → Cloudflare Queue → Postgres partitions.

## What exists (inspect it, then judge keep vs rebuild)
Monorepo: `apps/admin` (Next.js 15 App Router on Vercel, tRPC, Better Auth, Arabic RTL already present in places), `apps/api` (Hono on Cloudflare Workers), `packages/db` (Drizzle + Neon Postgres), `packages/emails` (react-email, Arabic RTL layout). Look at: `apps/admin/package.json`, `apps/admin/src` (app routes, components, styling, i18n/locale handling, data-fetching pattern, existing dashboard/stat components, tables, forms), tailwind/postcss/globals CSS, any `components/ui` (shadcn?) folder, the current orders list and order detail pages, and `apps/mobile` (Expo — the plan deletes it; note anything worth salvaging as a PWA pattern). Report concretely: what is reusable, what is a dead end, and why.

### Current state (verified 2026-09-24 on main — supersedes anything above it contradicts)
- **Shell:** `components/layout/CarbonShell.tsx` + `styles/carbon.scss` wrap every page in `@carbon/react` Header/SideNav, g100 dark theme (PR #363). Pages inside are still shadcn/Tailwind, so chrome and content are two design systems. **Owner decision: remove Carbon.** One system, no component-library swap.
- **Tokens:** `app/[locale]/globals.css` — dark-only navy `#060a10` + gold `#e09000`, no light theme, no `prefers-color-scheme`. Pages bypass tokens: ~1,722 inline `var(--x)` arbitrary values, 78 raw palette classes, 18 hex literals.
- **IA:** 25 dashboard route groups, `lib/navigation.ts` = 5 groups / 26 flat links, labels hard-coded Arabic; mobile = a Radix drawer below 66rem, no bottom tabs.
- **Components:** 23 in `components/ui` (shadcn primitives + KpiCard, StatBox, FormDialog, EmptyState, StatusBadge, PipelineBar, FilterTabs, …). Charts = two hand-rolled SVGs (`charts/BarChart.tsx`, `Sparkline.tsx`). No chart/table lib, no Playwright, no PWA.
- **i18n:** next-intl; batches A–D (#374–#377) externalized orders/products/customers/campaigns; ~700 lines of Arabic literals remain elsewhere. Digits: owner home now Western (#382); 17 other `ar-EG` Arabic-Indic sites remain.
- **Home today:** 4 KPI cards + pipeline bar + recent-orders table. Production DB currently has **no orders/inventory**, so every screen shows empty states — design the empty/first-run experience deliberately.
- **Owner decisions (2026-09-23):** no Chatwoot/Metabase/n8n — inbox, reports and integrations live inside this admin; real data will come from the Shopify order pipeline (S1), not demo seeds.
- **Who builds your packets:** UI packets go to an agent on the `ui` lane (agy) plus Stitch-generated screen references; write every packet so it is executable from the packet alone, with `data-testid` assertions.

Brand context (storefront brand bible — NOT to be copied literally into the admin): CALM · REFINED · TIMELESS; ivory #F3EFE7 / black #111111 / gold ≈#B0885E; Canela + Suisse Intl on the storefront. The admin is a working tool used 8h/day — it should feel like it belongs to the same house (restraint, warmth, precision) without pretending to be the storefront.

## Deliver these sections, in order

1. **Audit of `apps/admin`** — stack, structure, what to keep/rebuild, RTL/i18n state, data-layer verdict (tRPC in admin vs typed REST/OpenAPI shared with the public API), auth/session UX gaps.
2. **UI stack decision** — pick and justify (e.g., Tailwind v4 + shadcn/ui + Radix primitives, or alternatives); charting lib for the 8 reports (free, RTL-safe, small); table/virtualization lib; forms/validation; icons; date/number/currency formatting for ar-EG/en (digits policy: Western Arabic numerals for money everywhere? decide); PWA/installability approach for phones (replaces the Expo app); state/data-fetching conventions; testing stack for UI (Playwright + component tests). Give versions and cite official docs where it matters.
3. **Information architecture** — one navigation model that works on phone and desktop: role-specific shells (owner, accountant, ops/warehouse, POS, supplier portal) sharing one component system; the home screen for each role (owner home = exceptions + today + money; ops home = pick/pack/ship queue; accountant home = close checklist + unposted + reconciliation). Global: command palette, entity/brand/warehouse scope switcher, notifications, search. Mobile: bottom tab bar + sheets vs. sidebar; thumb-zone placement of primary actions.
4. **Design system spec** — tokens (light + dark; colors as OKLCH or hex with contrast ratios; a considered neutral with slight warm hue bias; ONE accent; a separate semantic set for success/warning/critical/info; status colors for the section-status vocabulary and for import-blocked/held/reserved/expired/quarantined), type scale (Arabic + Latin pairing — Google Fonts only, e.g. IBM Plex Sans Arabic / Noto Naskh Arabic / Cairo / Tajawal / Rubik — justify legibility at small sizes and in dense tables), spacing/radius/elevation, density modes (comfortable vs compact for the accountant), motion rules (reduced-motion respected, purposeful only), iconography, RTL rules (logical properties, mirrored-icon list, number direction, bidi in mixed strings), accessibility target (WCAG 2.2 AA; focus states; 44px targets), performance budget (LCP/INP targets on a mid-range Android on 4G).
5. **Component inventory** — the ~25 components that carry the product: Money (amount + currency + FX note), StatusPill (with the status vocabulary), SectionStatus banner, DataTable with mobile card fallback + saved filters, Timeline/Activity, EvidenceDrawer (raw webhook / source fetch / revisions), ApprovalSheet (propose → approve with reason), ScopeSwitcher, ExceptionCard, StockLotBadge (released/quarantined/expired/recalled), LotPicker (FEFO suggestion), AddressCard, ContactCard (guest vs profile), Command palette, Empty/Loading/Error states, Report chart frame (definition + source + freshness + coverage statement), Barcode/scan input for warehouse, Print templates (pick list, packing slip, receipt). For each: purpose, props at a high level, mobile behaviour, states.
6. **Screen program for Slice 1 (order-first)** — table per screen: purpose · primary action · data required (which API operations) · mobile layout · desktop layout · states (loading/empty/blocked/error) · acceptance assertions a Playwright test would make. Screens: Order Desk (list + saved views incl. "needs attention"), Order Workspace (the 11 parity sections in one scrollable workspace with a sticky action bar), Import Blocked resolver, Variant Mapping resolver, Customer profile (group-level person with per-brand relationship), Product/variant + per-channel listing, Stock (on-hand by owner/location/lot), Reserve → Pick → Pack → Ship flow (phone-first, scan-capable), Courier booking + tracking, COD remittance/settlement reconciliation, Approvals inbox, Audit log viewer with filters, Connections/settings (Shopify/Woo/courier/payment keys, health checks that test permissions not just HTTP 200), Owner home dashboard (which 3 of the 8 reports ship first), Login/2FA/recovery.
7. **Dashboard & data-viz rules** — chart type per report, one-scale rule, tabular numerals, no dual-axis, freshness + coverage statement placement, colour semantics separate from accent, how "missing costs are not zero costs" is shown.
8. **Anti-generic guardrails** — name the AI-design clichés to avoid (cream+serif+terracotta, purple gradients, everything-is-a-card, emoji section markers, centred everything) and state the specific visual identity of this admin in 5 sentences.
9. **Build program for the UI track** — 15–30 build packets sized S/M/L, each independently executable by a coding agent in a fresh context: title, goal, files/area, API dependencies (what must exist first), acceptance criteria incl. browser assertions, and which packets can run in parallel. Order them so the owner sees the real order rendered correctly as early as possible.
10. **What you would refuse to build now** — and why.

Cite official documentation for stack/version claims (Next.js, Tailwind, shadcn, Radix, WCAG 2.2, Apple HIG / Material 3 for mobile patterns, RTL styling guidance). Use tables. No filler.
