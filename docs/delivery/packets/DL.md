# reconcile:DL: DL — Delivery structure: one connected task system for Codex / Jules / Hermes / Claude (+ external UI track) working in parallel on irth-os

## Decisions
- **System of record for tasks** → GitHub Issues in sheiko0777/irth-os are the single connected structure: one issue = one task packet; one epic issue per slice (S0–S5) with the tasks attached as native sub-issues; depends_on expressed as native issue dependencies (blocked_by); executor/slice/domain/status as labels; one milestone per slice gate; one GitHub Project (v2) board 'IRTH OS Delivery' as the owner's view. No external tracker, no coordination JSON blocks, no local SQLite cache.  
  _A GitHub issue body is the only artifact all four executors can consume: Jules is triggered by the `jules` label and reads the issue text (jules.google/docs/running-tasks); Codex briefs are generated from the same body; Hermes is handed the issue URL; Claude reads it with gh. Native sub-issues + dependencies give the DAG without a second tool; the existing repo already uses gh, squash PRs and `Closes #n`._ [my_call] — jules.google/docs/running-tasks (label `jules`, Jules comments + links PR); jules.google/docs/usage-limits (Free 15/day, 3 concurrent; Pro 100/15); docs.github.com/en/rest/issues/issue-dependencies (POST /repos/{o}/{r}/issues/{n}/dependencies/blocked_by {issue_id}); gh label list on the repo shows only defaults + claude-code; `gh api repos/sheiko0777/irth-os/milestones` = [].
- **Reject heavier coordination machinery** → Do not adopt ECC /epic-decompose|publish|sync|claim|validate (coordination JSON block in issue body + `coordination:*` labels + SQLite store in the plugin cache), github-project-management (depends on `npx ruv-swarm`), claude-devfleet (needs a DevFleet MCP server on localhost:18801), or hermes-imports (an ECC skill-sanitizer, unrelated to running Hermes).  
  _Each adds a second state store or an external runtime that Jules/Codex cannot read; the same DAG/state is expressible with labels + native dependencies + milestones. Astra's whole critique is 'smaller machinery'; the delivery layer should follow it too._ [my_call] — C:/Users/sheri/.claude/plugins/cache/ecc/ecc/2.2.1/scripts/lib/github-coordination/policy.js (DEFAULT_LABELS coordination:*, epicOnly branch model, SQLite openStore); github-project-management/SKILL.md (ruv-swarm CLI throughout); claude-devfleet/SKILL.md ('Requires a running Claude DevFleet instance'); hermes-imports/SKILL.md.
- **Skills actually used to run the machine** → Adopt: configure-issue-tracker + brainstorm-to-issue + superpowers-issue-bridge (they define/read docs/agents/issue-tracker.md and the intent→spec→plan→PR trace), superpowers writing-plans + subagent-driven-development (Claude's own execution loop for exec:claude tasks and for producing bite-sized code-complete briefs), mattpocock to-tickets ticket shape (tracer-bullet vertical tickets + native blocked_by edges), codex-delegate (+ the codex model-tiering memory) for Codex dispatch, delegate-setup fleet lanes (delegate-fleet.v1 JSON) as the machine-readable half of the routing table for local CLIs (codex/opencode/kimi/grok/agy), gh-issues for Claude-worker fan-out on `fix/issue-<n>` batches when needed, and the repo's own new-migration + verify-gate skills.  
  _These are light, gh-based, and compose; the packet template is the union of what codex-delegate's brief, Jules' prompt guidance and writing-plans' task block each need, so one body serves every executor._ [my_call] — C:/Users/sheri/.claude/skills/{configure-issue-tracker,brainstorm-to-issue,superpowers-issue-bridge,gh-issues,codex-delegate,delegate-setup}/SKILL.md; codex-delegate/references/writing-the-brief.md (four XML blocks, real gate commands, 'do not commit'); superpowers/6.1.1/skills/{writing-plans,subagent-driven-development}/SKILL.md; mattpocock-skills/1.2.3/skills/engineering/to-tickets/SKILL.md; memory feedback_codex_model_tiering.md.
- **Task packet template (one body, all executors)** → Fixed sections, in this order: Title `[<PREFIX>-<nn>] <imperative>`; Goal (one paragraph, user-visible outcome); Slice / Domain / Executor / Size (labels mirror them); Context pointers (docs/agents/domain.md, the slice epic, decisions locked, owner facts A1–A4 when relevant); Files (exact paths, `new:` prefix for new files); Reuse (existing functions/patterns with paths — e.g. packages/db/src/ledger.ts postJournalEntry, packages/db/src/index.ts withOrgContext, apps/api/src/routes/webhooks/shopify.ts claimDelivery); Do-not-touch (paths + 'no migrations / no pnpm-lock / no .env / no auth config' unless exec:claude); Acceptance (checkable bullets); Tests (unit / integration-on-disposable-Postgres / RLS-coverage / Playwright, with file paths); Depends_on (issue refs → native blocked_by); Report contract (what the PR body must contain: evidence row, gate output counts, deviations). Jules reads the issue as-is; Codex gets the same body wrapped by scripts/delivery/packet-to-brief.mjs into <task>/<verification_loop>/<action_safety>/<structured_output_contract>; Hermes gets the issue URL + repo path; Claude runs it via writing-plans/subagent-driven-development.  
  _Jules' own guidance is 'specific and scoped' prompts; Codex sees only the brief; both need the real gate commands (verify-gate skill: node pnpm.cjs turbo lint typecheck test). Putting the report contract in the packet is what makes the evidence ledger fill itself._ [my_call] — E:/CLAUDE/jules-phase26-prompt.txt (the owner's existing Jules prompt style already has STRICT RULES / files-only / DO NOT touch); codex-delegate/references/writing-the-brief.md; .claude/skills/verify-gate/SKILL.md.
- **Routing table task-type → executor** → claude: anything under packages/db/drizzle/**, packages/db/src/schema/**, packages/db/src/index.ts (withOrgContext), RLS/tenancy gates, Better Auth, integration of others' PRs, every external-PR review. codex (gpt-5.6-sol default; gpt-6-astra only for RLS/race/security; terra for mechanical): ledger posting paths, WAC/reservation/lot allocation, ingestion receive→hydrate→promote, connector adapters, state machines, reconciliation, COD remittance matching. jules: isolated PR-sized work with no migration and no real-Postgres dependency — admin screens against existing tRPC/ops, CI/yaml, scripts, unit tests, refactors inside one workspace. hermes: research, docs, fixture masking, test scaffolds (Playwright/e2e), SQL report drafts, digests; never money paths. owner/accountant: decisions (recognition point, chart of accounts, transfer prices), the real Shopify order fixture (A4), UAT, slice-gate sign-off. ui (astra external): apps/admin presentation only, reviewed like any external PR, no server/schema changes. Any task that would touch two lanes is split; the schema half is always exec:claude.  
  _Matches the executor-fit rule in the brief and the repo's proven failure modes (tenancy leaks, money-as-float, migration ordering) which are exactly the Claude-reviewed lanes._ [all_agree] — Task rules in the brief; .claude/agents/money-reviewer.md + tenancy-reviewer.md; STATUS.md F11 note '0058 renumbered from 0057 (Jules generated it against an earlier main)'.
- **Branch naming, PR title, merge policy** → Branches `<exec>/<slice>-<id>-<slug>` (e.g. codex/s1-or-03-hydrate-order, claude/s0-dm-01-entity-tables, ui/s1-ui-02-order-workspace); Jules' generated branch names are accepted as-is (not controllable) — identity comes from the PR title `type(scope): summary [OR-03]` and `Closes #<issue>` in the body. Squash-only (disable merge commits and rebase merges; delete branch on merge). Required status checks on main: test, integration, secret-scan, migration-check, schema-guard. The `claude-code` label remains the ONLY merge trigger (owner's one click, on phone), but with required checks it now waits for green.  
  _Today `required_status_checks` is null and `required_approving_review_count` is 0, so `gh pr merge --squash --auto` on the label merges immediately even when CI is red — the one gate the owner relies on is not a gate. Keeping the owner's label as the trigger satisfies G10 (secure, one place) without a second approval mechanism._ [my_call] — `gh api repos/sheiko0777/irth-os/branches/main/protection --jq '{required_status_checks,reviews}'` → {null, 0}; `gh repo view --json mergeCommitAllowed,rebaseMergeAllowed,squashMergeAllowed,deleteBranchOnMerge` → true,true,true,false; .github/workflows/claude-code-automerge.yml.
- **Review protocol** → Every non-Claude PR (jules, codex, hermes, ui) gets a Claude review before the owner labels it: fresh worktree checkout, verify-gate run, `git diff origin/main...HEAD` handed to the existing money-reviewer and tenancy-reviewer subagents, packet-compliance check (touched files ⊆ packet Files, Do-not-touch honored, acceptance evidenced, evidence row present), then `gh pr review --approve|--request-changes` and label review:approved|review:changes-requested. Two rounds of changes-requested → Claude takes the task over. Codex cloud `@codex review` is an optional second opinion (owner decision).  
  _Astra: 'for one owner, an explicit human approval can be sufficient; do not pretend a second agent constitutes independent approval' — so Claude reviews, the owner approves. The two reviewer agents already encode the repo's real failure classes._ [all_agree] — Astra critique §E (security minimum) and §G risk 5; memory project_irth_os_multi_agent_lead.md (owner asked Claude to be senior reviewer over other tools); .claude/agents/*.md; learn.chatgpt.com/docs/third-party/github (`@codex review` requires Codex cloud set up for the repo).
- **Conflict avoidance** → (1) Migrations are written only in exec:claude tasks; packets for jules/codex/hermes say 'no migrations'. Schema needs from other lanes become a `needs:schema` label + a blocking DM/CX task owned by Claude. (2) CI migration-check: filenames `NNNN_snake.sql`, unique prefixes, new files must be numbered above origin/main's max, existing files must not be edited/deleted (migrate.mjs keys its ledger on filename). (3) CI schema-guard: PR touching packages/db/drizzle/**, packages/db/src/schema/**, packages/db/src/index.ts or the gate tests must carry exec:claude or schema:approved; exec:ui PRs may not touch apps/admin/src/server/**. (4) File ownership per lane is declared in each slice epic's Lanes table and in each packet's Files/Do-not-touch. (5) One git worktree per dispatch (E:/irth-wt/<branch>); E:/irth-os stays a read-only main mirror; never run manual git in a worktree an agent is using.  
  _The numbering collision and the stale-file regression (F01 process note) both already happened; the checks are small scripts; ownership-by-packet is cheaper than CODEOWNERS for a single-owner repo._ [my_call] — docs/implementation/STATUS.md F11 row and F01 process note; packages/db/scripts/migrate.mjs header; .claude/hooks/protect-paths.mjs; memory project_irth_os_pnpm_hazard.md (worktree race, gh pr merge --delete-branch caveat).
- **Slice gates and evidence** → A slice closes when its epic's gate checklist is satisfied (all sub-issues closed, CI green on main, Playwright suite for the slice green, owner UAT of the complete transaction, accountant sign-off where money posts) — no calendar dates. Evidence rows keep the existing STATUS.md shape `ID | status | PR | commit | evidence/tests | migration | blocker | next` but live in docs/delivery/<slice>.md; docs/implementation/STATUS.md and BASELINE.md are frozen with a banner (BASELINE's 'live production, 3 tenants, 1000 orders/day' contradicts the owner's locked fact 'not in production').  
  _Astra: 'acceptance gates should replace optimistic calendar estimates'; the 66 KB STATUS.md is already hard to maintain; the row format itself is good and agents already know it._ [all_agree] — docs/implementation/STATUS.md header 'Progress row format'; docs/implementation/BASELINE.md scale row; astra §G risk 5.
- **Daily sync ritual and owner control surface** → Replace the session-only 7-day cron with a durable scheduled Claude routine (schedule skill) at 09:00 Africa/Cairo: sweep open PRs by lane + CI state, main deploy health, blocked issues (issue_dependencies_summary.blocked_by>0 or status:blocked), Jules WIP vs its concurrency cap, needs:owner/needs:accountant decisions, migration queue; write the 'irth-os Watch' artifact via Artifact write_db (state/dashboard); post a digest comment on the active slice epic; append evidence rows for PRs merged since last run. Owner triggers work from the phone by (a) applying `jules` (Jules lane), (b) commenting `dispatch codex` / `dispatch hermes` on the issue (picked up by Claude's sync or an interactive session), (c) applying `claude-code` to merge; owner answers decisions by closing decision issues with a comment.  
  _The dashboard and its write_db refresh already exist; only the scheduler is fragile. GitHub labels/comments are the only control surface that works from a phone for all executors._ [my_call] — memory project_irth_os_multi_agent_lead.md (artifact URL, write_db refresh, cron 62582537 session-only); schedule skill available in this environment.
- **Jules lane constraints** → Jules tasks: single workspace, no migrations, no pnpm-lock/.env/auth changes, unit tests only (Jules' VM has no disposable Postgres; integration/RLS tests run in CI on its PR), PR title carries the packet id, body has `Closes #n`. Owner-applied `jules` label only (or Claude); cap in-flight Jules tasks at the plan's concurrency (3 on Free). Jules' Sentinel/Bolt scheduled tasks keep running; their learnings files (.jules/bolt.md, sentinel.md) stay.  
  _Jules reads AGENTS.md and issue text only; its limits are published; the owner's existing Jules prompts already enforce the same restrictions._ [my_call] — jules.google/docs/usage-limits; github.com/google-labs-code/jules-action README (issue-triggered workflows must be restricted to trusted users; inputs prompt/jules_api_key/starting_branch); .jules/*.md; open PR #339 (Sentinel branch).
- **Codex packaging** → Default dispatch is local: Claude generates the brief from the issue (packet-to-brief.mjs) and runs codex-delegate's relay in a fresh worktree with dependencies pre-installed; Codex never commits; Claude reviews, runs the gate, commits, opens the PR under codex/<branch>. Model tier chosen per packet (sol default for money/connector; astra only for RLS/race; terra/luna for mechanical). Codex cloud (@codex on issues/PRs, automatic PR review) is optional and gated on the owner setting up the cloud environment.  
  _Matches the proven loop already used on this repo (STATUS.md rows cite orchestrator re-runs) and the owner's cost-tiering instruction; avoids exposing DB secrets to a cloud env until needed._ [my_call] — learn.chatgpt.com/docs/non-interactive-mode (`codex exec`, --json, -o, --sandbox, --output-schema); learn.chatgpt.com/docs/cloud and /docs/third-party/github; codex-delegate/SKILL.md; memory feedback_codex_model_tiering.md; `codex --version` = codex-cli 0.153.4.
- **Hermes lane** → Hermes (Nous hermes-agent, desktop/self-hosted) is a research/docs/tests/fixtures lane only, probed once before any packet is routed to it; it never touches money paths or schema. If the probe shows it can push branches and open PRs, it uses hermes/<branch>; otherwise it returns files that Claude commits.  
  _Hermes docs describe webhook/cron-triggered GitHub reports, PR review summaries, CI-failure analysis and (in a demo) opening PRs, but no label trigger and no sandboxed test runner comparable to Jules/Codex; capability must be verified, not assumed._ [my_call] — hermes-agent.nousresearch.com/docs (cron, skills, gateways); hermes-agent.ai/integrations/github (triage, PR review summaries, Actions failure inspection, 'start with read-only access').
- **S0 test infrastructure DL must provide** → Two harness gaps block S1 acceptance and are DL's to close in S0: (1) apps/api has no real-Postgres integration suite (all mocked), yet S1 ingestion promote/exception paths must be proven against constraints/RLS; (2) no Playwright at all, yet the S1 gate requires browser assertions of the 11 parity sections. Both share the single Neon integration branch and must run inside the existing `shared-neon-integration-branch` concurrency group.  
  _Astra §B: browser tests must assert visible items/buyer/address/payment/fulfillment/evidence; failure tests (crashes, duplicates, failed child writes) need a real database. BASELINE.md already flagged the apps/api gap._ [all_agree] — .github/workflows/ci.yml (integration job, concurrency group comment); apps/api/package.json test = vitest run (mocked); no apps/admin/playwright.config.ts; docs/implementation/BASELINE.md 'apps/api has no real-Postgres integration test suite'.
- **Automation security** → Labels `jules`, `exec:*`, `schema:approved`, `claude-code` are applied only by the owner or Claude; any workflow triggered by issues/labels/comments that mutates anything gates on `github.actor == 'sheiko0777'`; issue/PR text is untrusted data for every agent; no secrets or PII in issues/PRs (fixtures masked per A4); Jules'/Codex' environments get no DATABASE_URL — integration tests only run in CI with TEST_DATABASE_URL.  
  _Public repo + label-triggered agents = prompt-injection and quota-drain surface; jules-action's README says exactly this._ [all_agree] — `gh repo view --json isPrivate` → false; github.com/google-labs-code/jules-action security section; github-project-management/SKILL.md security section; astra §E ('treat imported notes… as untrusted data').

## Keep
- CI gate `pnpm turbo lint typecheck test` + integration job on one shared disposable Neon branch with the `shared-neon-integration-branch` concurrency group @ `.github/workflows/ci.yml` — Real-Postgres gates are what catch RLS/constraint/idempotency defects; the concurrency group is a hard-won fix (PR #211 race).
- TruffleHog secret scan, deploy-api/deploy-admin (gates → migrate.mjs → wrangler/vercel → health smoke), deploy-all @ `.github/workflows/secret-scan.yml, deploy-api.yml, deploy-admin.yml, deploy-all.yml` — Already prove boot, not just upload; migrations run on packages/db/** changes.
- `claude-code` label auto-merge (squash, --auto) @ `.github/workflows/claude-code-automerge.yml; CLAUDE.md 'Branch & Merge Workflow'` — It is the owner's one-click phone control; only needs required checks underneath it.
- Three structural gates: tenancyGate (static router scan), rlsCoverage (catalogue-driven, real Postgres), schemaDrift (NOT NULL columns vs Drizzle), plus moneyColumns ratchet and shopifyLockOrderGate @ `apps/admin/src/__tests__/tenancyGate.test.ts, apps/admin/src/__tests__/integration/{rlsCoverage,schemaDrift}.test.ts, packages/db/src/__tests__/moneyColumns.test.ts` — They police agent output automatically; every new table/policy from DM tasks is checked without anyone remembering.
- Custom migration runner and its filename-keyed ledger @ `packages/db/scripts/migrate.mjs; .claude/skills/new-migration/SKILL.md` — DL-05 builds the numbering lock on top of its ordering semantics; do not switch to drizzle's migrator.
- Editing hooks and reviewer agents @ `.claude/hooks/protect-paths.mjs, .claude/hooks/money-gate.mjs, .claude/agents/money-reviewer.md, .claude/agents/tenancy-reviewer.md (one stale sentence to fix), .claude/skills/verify-gate/SKILL.md` — They are the review protocol's tools; verify-gate encodes the Windows pnpm.cjs invocation every brief must copy.
- CLAUDE.md five rules and AGENTS.md layout/conventions (incl. 'no Co-Authored-By trailer', conventional commits) @ `CLAUDE.md, AGENTS.md` — Jules and Codex both read AGENTS.md automatically; the packet only restates the load-bearing rules.
- Jules scheduled personas and their learnings files @ `.jules/bolt.md, .jules/sentinel.md; PRs #306,#313,#332,#338,#339` — Cheap continuous security/perf sweeps that already found real authz bypasses; keep them, route their PRs through the same review protocol.
- STATUS evidence-row format @ `docs/implementation/STATUS.md header` — Precise, already understood by agents; moves to docs/delivery/<slice>.md.
- 'irth-os Watch' dashboard artifact and its write_db refresh path @ `https://claude.ai/code/artifact/9032e94b-f067-402d-9ba2-f7f5a49f348c (state/dashboard)` — Owner's phone-first monitor already exists; only the scheduler needs to become durable.
- codex-delegate relay loop and the model-tiering rule @ `C:/Users/sheri/.claude/skills/codex-delegate (scripts/relay.mjs, references/*.md); memory feedback_codex_model_tiering.md` — Proven on this repo (F-series rows); orchestrator-commits keeps Codex out of .git.
- Worktree layout: E:/irth-os on main, task worktrees elsewhere, pnpm.cjs invocation, install-before-delegate rule @ `memory project_irth_os_pnpm_hazard.md; git worktree list` — A killed pnpm install corrupts node_modules; a shared worktree raced once and produced an empty PR branch.

## Cut
- ECC epic coordination (/epic-decompose, /epic-publish, /epic-sync, /epic-claim, /epic-validate, /epic-review, /epic-unblock) and its `coordination:*` labels / issue-body JSON block / SQLite store — Second state store outside GitHub that Jules/Codex cannot read; epic-only branch model conflicts with per-task PRs; native sub-issues + dependencies cover the DAG.
- github-project-management skill and /github:issue-tracker command — Both are ruv-swarm / claude-flow dependent (not installed, not needed); their templates are swarm-flavoured boilerplate.
- claude-devfleet — Requires a running DevFleet MCP server; Claude's own subagent-driven-development + worktrees already give per-task isolation.
- hermes-imports — It sanitizes Hermes workflows into ECC skills; it does not run Hermes as a coding agent.
- Pasting hand-written phase prompts (E:/CLAUDE/jules-phaseNN-prompt.txt, pNN-*-prompt.txt) into Jules — Replaced by the issue packet + `jules` label; the prompt text lives in the issue where the PR links back to it.
- Session-only Claude cron (job 62582537) as the daily sweep — Expires with the session / after 7 days; replaced by a durable scheduled routine.
- Merge commits and rebase merges on main; `delete_branch_on_merge=false` — Squash-only keeps one commit per packet and makes evidence rows map 1:1 to commits; stale branches already litter the repo (30+).
- CODEOWNERS / required reviewers — Single human owner; review is Claude's protocol + the owner's label, not GitHub review counts.
- Growing docs/implementation/STATUS.md (66 KB) and BASELINE.md as live documents — Frozen as history; BASELINE's 'live production' premise contradicts the locked owner facts.
- A generic 'files-touched ⊆ packet' enforcement bot — YAGNI: the two real conflict classes (migration numbering, schema ownership) get blocking checks; the rest is a review-protocol checkbox.
- Codex cloud environment as the default Codex path — Needs a 10–30 min pnpm install baked into a cloud env and repo secrets exposure; local relay in a pre-installed worktree is proven. Kept as optional (DL-19).

## Tasks
### DL-01 [S0/S/claude] Bootstrap docs/agents: tracker config, domain map, routing table, packet template
- goal: Create the docs/agents/ folder that the issue-bridge skills expect and that every packet's 'Context pointers' section links to, so a fresh agent can read prefixes, slices, executor routing, lane rules and the packet template in one place.
- depends_on: —
- files: new: docs/agents/issue-tracker.md (Backend GitHub, Location sheiko0777/irth-os, Labels: task, intent), new: docs/agents/domain.md (prefix table DM/OR/IN/CX/AN/DL/UI, slice IDs S0–S5 with one-line gate each, executor routing table task-type → executor incl. Codex tier rule, size rules S/M/L, the locked decisions list pointer, owner facts A1–A4), new: docs/agents/task-packet.md (the packet sections in order: Title, Goal, Slice/Domain/Executor/Size, Context pointers, Files, Reuse, Do-not-touch, Acceptance, Tests, Depends_on, Report contract), new: docs/agents/triage-labels.md (label vocabulary: exec:claude|codex|jules|hermes|owner|accountant|ui, slice:S0..S5, domain:DM|OR|IN|CX|AN|DL|UI, status:ready|blocked|in-review, needs:owner|accountant|schema, review:approved|changes-requested, schema:approved, jules, claude-code, intent), AGENTS.md (append 'Agent configuration' + 'Workflow: intent → packet → PR' sections per configure-issue-tracker; do not touch CLAUDE.md)
- acceptance: docs/agents/issue-tracker.md follows the configure-issue-tracker shape (Backend/Location/Labels/Notes) and names sheiko0777/irth-os | docs/agents/domain.md contains the routing table with every executor and the 'schema half is always exec:claude' rule | task-packet.md lists all 11 sections and a filled example packet (use DL-05 as the example) | AGENTS.md links to docs/agents/* and states that Jules/Codex must read the packet's Do-not-touch list | prettier (`pnpm format` check) clean; CLAUDE.md byte-identical
- tests: none beyond `pnpm format --check` on the touched .md files
- risks: Keep AGENTS.md edits surgical — Jules and Codex parse it automatically.
### DL-02 [S0/S/claude] Idempotent GitHub bootstrap: labels, milestones, project board
- goal: One script the owner or Claude can run twice to converge the repo on the label vocabulary, six slice-gate milestones, and the 'IRTH OS Delivery' Project board with Executor/Slice/Status fields.
- depends_on: DL-01
- files: new: scripts/delivery/gh-bootstrap.mjs (Node 20, uses `gh` via child_process; --dry-run; reads label list from docs/agents/triage-labels.md or an inline table; creates labels with fixed colours; milestones 'S0 foundation gate' … 'S5 platform gate' (no due dates); `gh project create --owner sheiko0777 --title 'IRTH OS Delivery'` + `gh project field-create` for Executor, Slice, Status single-selects), docs/agents/merge-policy.md (prerequisite line: `gh auth refresh -s project,read:project`)
- acceptance: `node scripts/delivery/gh-bootstrap.mjs --dry-run` prints the full plan and changes nothing | first real run creates all labels/milestones/project; second run reports 0 changes | `gh label list -R sheiko0777/irth-os` shows every label in triage-labels.md; `gh api repos/sheiko0777/irth-os/milestones` returns 6 | never deletes existing labels (bug, claude-code, etc.)
- tests: scripts/delivery/__tests__/gh-bootstrap.test.mjs (node:test): diff planner given current-vs-desired label sets yields create/update/no-op lists
- risks: gh token currently lacks read:project scope (verified: `gh project list` errors) — the doc must say to refresh scopes first.
### DL-03 [S0/S/jules] Issue forms and PR template
- goal: GitHub issue forms that produce packets in the exact docs/agents/task-packet.md shape (so Jules/Codex/Hermes get a uniform body) plus a PR checklist that forces the evidence row and gate proof.
- depends_on: DL-01
- files: new: .github/ISSUE_TEMPLATE/task-packet.yml (form: id, goal, slice dropdown S0–S5, domain dropdown, executor dropdown, size dropdown, context pointers, files, reuse, do-not-touch, acceptance, tests, depends_on, report contract; auto-labels: task), new: .github/ISSUE_TEMPLATE/slice-epic.yml (goal, gate checklist, lanes/file-ownership table, milestone), new: .github/ISSUE_TEMPLATE/decision.yml (question, options, default-if-unanswered, needed-by task ids; auto-label needs:owner or needs:accountant), new: .github/ISSUE_TEMPLATE/config.yml (blank_issues_enabled: false), new: .github/PULL_REQUEST_TEMPLATE.md (checklist: packet id in title, `Closes #n`, gate command + counts, migration? numbered above main / schema updated / new-migration skill, RLS policy for new tables, money bigint, writes via withOrg, evidence row per docs/delivery/README.md, 'proved the gate fails' for new gates, Do-not-touch honored, screenshots for UI)
- acceptance: `gh issue create --web` shows the three forms; task-packet form fields match task-packet.md 1:1 and in order | PR template renders on a new PR; contains the evidence-row table header verbatim from docs/delivery/README.md | no migrations, no lockfile, no changes outside .github/
- tests: YAML parse check: `node -e "require('yaml')..."` is not available — use `node --input-type=module -e` with a minimal parser or rely on GitHub rendering; acceptance is verified by opening one test issue from each form and closing it
- risks: Jules task: keep to .github/ only; Claude reviews rendering.
### DL-04 [S0/S/claude] Merge policy: required status checks, squash-only, branch cleanup
- goal: Make the `claude-code` label a real gate: main requires test, integration, secret-scan, migration-check, schema-guard; merge commits/rebase disabled; branches auto-deleted.
- depends_on: DL-05, DL-06
- files: new: docs/agents/merge-policy.md (exact `gh api -X PUT repos/sheiko0777/irth-os/branches/main/protection` payload with required_status_checks.contexts, enforce_admins true, `gh repo edit --enable-merge-commit=false --enable-rebase-merge=false --delete-branch-on-merge`, and the rule 'owner's claude-code label is the only merge trigger'), .github/workflows/ci.yml (only if job names need stabilising: `test`, `integration`), .github/workflows/secret-scan.yml (job name `secret-scan`)
- acceptance: `gh api repos/sheiko0777/irth-os/branches/main/protection --jq .required_status_checks.contexts` = [test, integration, secret-scan, migration-check, schema-guard] | `gh repo view --json mergeCommitAllowed,rebaseMergeAllowed,deleteBranchOnMerge` → false,false,true | probe: a branch with one deliberately failing unit test, labelled claude-code, stays unmerged (auto-merge pending) until the test is fixed; probe PR then closed | owner explicitly approved the settings change (comment on the DL-04 issue)
- tests: the probe PR is the test; record its number in docs/delivery/S0.md
- risks: Changes repo settings — requires owner's go-ahead in the issue before running.
### DL-05 [S0/S/jules] Migration numbering lock (CI check `migration-check`)
- goal: Fail any PR whose migration files collide, are numbered at or below origin/main's highest, are malformed, or edit/delete an existing migration — the collision class that already forced the 0057→0058 renumber.
- depends_on: —
- files: new: packages/db/scripts/check-migrations.mjs (pure function `plan(mainFiles, prFilesWithStatus)` + CLI: lists packages/db/drizzle/*.sql on origin/main via `git ls-tree origin/main`, diffs `git diff --name-status origin/main...HEAD -- packages/db/drizzle`, rules: /^\d{4}_[a-z0-9_]+\.sql$/, unique 4-digit prefix across main+PR, added files' prefix > max(main), any M/D/R status under drizzle/ → fail with file names), packages/db/package.json (script `check:migrations`), .github/workflows/ci.yml (new job `migration-check`, actions/checkout fetch-depth 0, `git fetch origin main`, run the script; no pnpm install needed), new: packages/db/src/__tests__/checkMigrations.test.ts (unit tests of `plan`)
- acceptance: on current main the check passes | planted `0066_dup.sql` next to `0066_two_factor_lockout_columns.sql` → red naming both files; planted one-line edit to 0010 → red; planted `0060_x.sql` (≤ main max) → red; then plants reverted (proves the gate fails) | check appears as `migration-check` on the PR and is listed in DL-04's required contexts
- tests: packages/db/src/__tests__/checkMigrations.test.ts: cases unique/dup/edited/deleted/low-number/malformed
- risks: Do not touch migrate.mjs; the runner's filename ordering is the contract this check protects.
### DL-06 [S0/S/jules] Schema-path ownership guard (CI check `schema-guard`)
- goal: Block PRs that change schema/RLS/tenancy-critical paths unless the PR is an exec:claude task or Claude has applied schema:approved; block exec:ui PRs that touch server code.
- depends_on: DL-02
- files: new: .github/workflows/schema-guard.yml (on pull_request types [opened, synchronize, labeled, unlabeled]; permissions pull-requests: read; `gh pr view $N --json files,labels --jq` only — never interpolate body text; guarded paths: packages/db/drizzle/**, packages/db/src/schema/**, packages/db/src/index.ts, packages/db/src/orgContext.ts, packages/db/src/permissions.ts, apps/admin/src/__tests__/tenancyGate.test.ts, apps/admin/src/__tests__/integration/rlsCoverage.test.ts, apps/admin/src/__tests__/integration/schemaDrift.test.ts; pass if label exec:claude or schema:approved; extra rule: label exec:ui + any file under apps/admin/src/server/** → fail), docs/agents/merge-policy.md (one paragraph describing the guard and how Claude grants schema:approved)
- acceptance: probe PR from a non-claude branch touching packages/db/src/schema/index.ts without label → red; adding schema:approved re-triggers → green; probe closed | job name `schema-guard` matches DL-04's required contexts | workflow contains no `${{ github.event.pull_request.body }}` or title interpolation
- tests: two probe PRs (documented in docs/delivery/S0.md)
- risks: Label-triggered re-run must use `labeled`/`unlabeled` events or the check never turns green after approval.
### DL-07 [S0/M/claude] apps/api real-Postgres integration harness in CI
- goal: Give the API workspace the same disposable-Postgres test path apps/admin has, so S1 ingestion (receive→hydrate→promote, exceptions, duplicates, failed child writes) can be proven against constraints and RLS rather than mocks.
- depends_on: —
- files: new: apps/api/vitest.integration.config.ts (mirror apps/admin/vitest.integration.config.ts: include src/__tests__/integration/**/*.test.ts, globalSetup), new: apps/api/src/__tests__/integration/globalSetup.ts (reuse the `_integration_marker` claim pattern from apps/admin/src/__tests__/integration/globalSetup.ts; refuse if TEST_DATABASE_URL equals DATABASE_URL), new: apps/api/src/__tests__/integration/helpers/testDb.ts (copy of apps/admin's helper: testDb, truncateAll, closeTestDb), new: apps/api/src/__tests__/integration/webhookInbox.test.ts (seed test: claimDelivery from apps/api/src/routes/webhooks/shopify.ts round-trips received→retry→processed on real Postgres), apps/api/package.json (script test:integration), .github/workflows/ci.yml (integration job: after `pnpm --filter @irth/admin test:integration`, run `pnpm --filter @irth/api test:integration` in the same job so the shared-branch concurrency group still serialises), AGENTS.md (one line: apps/api integration suite exists, how to run)
- acceptance: CI integration job runs both suites serially and stays in `shared-neon-integration-branch` | running with TEST_DATABASE_URL == DATABASE_URL aborts before any truncate | seed test green; existing rlsCoverage/schemaDrift unaffected | the OR domain's S1 ingestion tests have a documented home: apps/api/src/__tests__/integration/
- tests: apps/api/src/__tests__/integration/webhookInbox.test.ts
- risks: Two suites truncating one branch — must never run in parallel jobs; keep them in one job.
### DL-08 [S0/M/hermes] Playwright scaffold + CI job for the admin
- goal: Stand up the browser-test harness the S1 gate needs (assert visible items, buyer, address, payment, fulfillment, evidence on the order workspace) with one smoke spec, so OR/UI tasks only add specs.
- depends_on: DL-07
- files: new: apps/admin/playwright.config.ts (baseURL http://localhost:3000, webServer `next dev` or `next start`, retries 1, trace on-first-retry), new: apps/admin/e2e/seed.ts (seeds one org + owner user via @irth/db against TEST_DATABASE_URL using the integration helpers; idempotent), new: apps/admin/e2e/smoke.spec.ts (sign in via the existing Better Auth route, open /[locale]/orders, assert the list heading renders), apps/admin/package.json (devDependency @playwright/test; script test:e2e), new: .github/workflows/e2e.yml (pull_request; concurrency group `shared-neon-integration-branch`, cancel-in-progress false; installs browsers; uploads playwright-report artifact; NOT a required check until the S1 gate), docs/agents/testing.md (how S1 parity specs should be written: one spec per parity section, data-testid contract `order-section-<name>` and `order-section-<name>-status`)
- acceptance: `pnpm --filter @irth/admin test:e2e` passes locally against a seeded disposable branch and in CI | e2e job shares the integration concurrency group (no parallel truncation) | docs/agents/testing.md defines the data-testid contract that UI/OR tasks must implement | pnpm-lock.yaml updated only by a real install run by Claude (Hermes returns package.json change + specs; Claude runs the install)
- tests: apps/admin/e2e/smoke.spec.ts
- risks: Requires an install (10–30 min on the owner's machine) — Claude runs it, never the delegated agent; Windows pnpm.cjs invocation per verify-gate.
### DL-09 [S0/S/jules] packet-to-brief: generate Codex briefs from issues
- goal: Turn any packet issue into a codex-delegate brief with the repo's real gate commands, action-safety rules and report contract, plus a suggested model tier, so Codex dispatch is one command.
- depends_on: DL-01
- files: new: scripts/delivery/packet-to-brief.mjs (CLI: `node scripts/delivery/packet-to-brief.mjs <issue> [--out <file>]`; reads `gh issue view <n> --json title,body,labels`; pure `render(packet)` returns text with <task> (goal, files, reuse, do-not-touch), <verification_loop> (exact commands from .claude/skills/verify-gate/SKILL.md: node pnpm.cjs turbo lint typecheck test, plus per-package filters), <action_safety> (no git add/commit, no pnpm install, no migrations unless label exec:claude, no .env/lockfile), <structured_output_contract> (1 what/why, 2 files, 3 gate counts, 4 deviations + the evidence row); prints `tier:` line: astra if labels include risk:rls|risk:race, sol if domain:DM/OR/IN/CX money or connector, else terra), new: scripts/delivery/__tests__/packet-to-brief.test.mjs (node:test with a fixture body), new: docs/agents/codex.md (dispatch: `node <codex-delegate skill dir>/scripts/relay.mjs --brief <file> --cd E:/irth-wt/<branch>`; pre-install rule; review + commit by Claude; tier rule copied from memory)
- acceptance: for a fixture packet the output has all four blocks, the exact verify-gate commands, and no text from outside the packet | tier line present and deterministic | never writes secrets; brief file goes to the scratch dir, not the repo
- tests: scripts/delivery/__tests__/packet-to-brief.test.mjs
- risks: Keep the script dependency-free (node:child_process + gh) so it runs on Windows without an install.
### DL-10 [S0/S/claude] Lane rules in AGENTS.md, worktree doc, reviewer-agent fix
- goal: Make the executor constraints readable by Jules/Codex where they actually look (AGENTS.md), document the worktree discipline, and fix the stale tenancy-reviewer text.
- depends_on: DL-01
- files: AGENTS.md (new section 'Agent lanes': Jules — one workspace per PR, no migrations/lockfile/.env/auth config, unit tests only, PR title `[ID]`, `Closes #n`, run the gate; Codex — same plus 'dependencies are pre-installed, never run pnpm install, do not commit'; Hermes — docs/tests/fixtures only; UI — apps/admin presentation only; Claude — schema/RLS/auth/integration/reviews), new: docs/agents/worktrees.md (E:/irth-os = read-only main mirror; one worktree per dispatch `git worktree add E:/irth-wt/<branch> -b <branch> origin/main`; pnpm.cjs invocation; install-before-delegate; `gh pr merge --delete-branch` caveat; detach after merge), new: .jules/README.md (what bolt.md/sentinel.md are; they are Jules' learnings files, keep appending), .claude/agents/tenancy-reviewer.md (replace 'There is no RLS yet…' with: RLS exists since 0031 and is forced; writes still must go through withOrg because the pooled role has BYPASSRLS — see tenancyGate.test.ts)
- acceptance: AGENTS.md 'Agent lanes' present; CLAUDE.md untouched | tenancy-reviewer.md no longer claims RLS is absent | docs/agents/worktrees.md contains the exact commands from the pnpm-hazard memory
- tests: none (docs); `pnpm format --check`
- risks: none
### DL-11 [S0/S/owner] Jules configuration + label-trigger probe
- goal: Confirm the Jules GitHub app, environment setup and plan limits on sheiko0777/irth-os, and prove the packet→`jules` label→PR path end to end on a real DL task.
- depends_on: DL-02, DL-03, DL-10
- files: Jules UI: repo connected; environment setup script `corepack enable && CI=true pnpm install --frozen-lockfile` (Node 20); snapshot saved, new: docs/agents/jules.md (written by Claude from the owner's findings: plan tier + limits, what Jules cannot do — merge, run real-Postgres tests, choose branch names, read secrets; how to trigger; WIP cap)
- acceptance: applying `jules` to the DL-03 issue makes Jules comment on the issue and open a PR whose body contains `Closes #<DL-03 issue>` | CI (test/integration/secret-scan/migration-check/schema-guard) ran on the Jules PR | docs/agents/jules.md records the confirmed tier (Free 15/day+3 concurrent or Pro 100/15) and the WIP cap used by DL-14
- tests: the probe PR
- risks: If the Jules app is installed on E:/irth-os's remote only for some branches, fix the app's repo access first.
### DL-12 [S0/S/owner] Hermes lane probe + capability doc
- goal: Verify what the owner's Hermes desktop agent can actually do against this repo (clone, run unit tests, push a branch, open a PR) before routing any packet to it, and document the hand-off.
- depends_on: DL-01
- files: new: docs/agents/hermes.md (capability matrix can/cannot from the probe; hand-off = paste the issue URL + repo path; expected output branch `hermes/<slice>-<id>-<slug>` + PR, else files attached to the issue for Claude to commit; allowed categories: research, docs, fixture masking, e2e/test scaffolds, SQL report drafts; forbidden: money paths, schema, connectors)
- acceptance: probe packet (e.g. 'write docs/agents/testing.md skeleton') completed by Hermes; result path (PR or files) recorded in docs/agents/hermes.md | exec:hermes is only applied to categories proven in the probe
- tests: none
- risks: Hermes docs show cron/webhook GitHub workflows and PR-review summaries but no sandboxed test runner; if it cannot open PRs, Hermes stays a files-to-Claude lane.
### DL-13 [S0/M/claude] review-external-pr skill and review protocol
- goal: A repeatable Claude procedure that reviews every non-Claude PR with the money-reviewer and tenancy-reviewer agents, checks packet compliance, posts a GitHub review and applies review labels — the human-in-the-loop gate before the owner's `claude-code` click.
- depends_on: DL-02, DL-04
- files: new: .claude/skills/review-external-pr/SKILL.md (steps: `gh pr view <n> --json headRefName,files,labels,body`; `git worktree add E:/irth-wt/pr-<n>` + `gh pr checkout`; run verify-gate; write `git diff origin/main...HEAD -U10` to a scratch file; dispatch money-reviewer and tenancy-reviewer (sonnet) on that file; if schema-guard paths touched run the schema checklist (org_id NOT NULL+index, RLS enable/force/policy, migration numbered above main, Drizzle schema updated); packet compliance (files ⊆ Files, Do-not-touch, acceptance evidenced, evidence row); `gh pr review --approve|--request-changes --body <file>`; `gh pr edit --add-label review:approved|review:changes-requested`; never add claude-code; two-strike rule → reassign to exec:claude), new: docs/agents/review-protocol.md (who/when/SLA same day; what each reviewer looks for; escalation)
- acceptance: run end-to-end on open PR #339 (Sentinel shipping RBAC): review comment contains both reviewer verdicts (clean or findings with file:line) and the label is applied | the skill refuses to run on a PR whose branch is checked out in another worktree
- tests: dry run on #339 recorded in docs/delivery/S0.md
- risks: Reviewer agents are advisory; the skill must run the real gate itself, never trust the PR's self-report.
### DL-14 [S0/S/claude] Durable daily sync routine feeding the irth-os Watch dashboard
- goal: Replace the session-only sweep with a scheduled Claude routine that keeps the owner's phone dashboard current and posts one digest per day where the work lives (the active slice epic).
- depends_on: DL-02, DL-13, DL-15
- files: new: docs/agents/daily-sync.md (the routine prompt, stored for reproducibility: 1 open PRs grouped by exec label with age + check status, 2 last Deploy API/Admin run state on main, 3 issues status:blocked or with issue_dependencies_summary.blocked_by>0, 4 Jules in-flight vs cap from docs/agents/jules.md, 5 open needs:owner/needs:accountant decisions, 6 PRs touching packages/db/drizzle (migration queue), 7 `Artifact write_db set collection state doc_id dashboard`, 8 digest comment on the active slice epic, 9 evidence rows for PRs merged since last run appended to docs/delivery/<slice>.md via a claude/ PR), scheduled routine created with the schedule skill (daily 09:00 Africa/Cairo); delete session cron 62582537
- acceptance: routine exists and one run completed; the Watch artifact shows sections Lanes, Blocked, Decisions, Migration queue, Jules WIP | digest comment posted on the S0 epic | old cron deleted
- tests: manual first run; second run shows 'no changes' when nothing moved
- risks: The routine must only append rows via PR (main is protected); it must never label claude-code.
### DL-15 [S0/S/jules] Evidence ledger per slice; freeze STATUS.md/BASELINE.md
- goal: Keep the proven evidence-row format but stop growing the 66 KB STATUS.md; give each slice its own ledger that PRs and the sync routine append to.
- depends_on: DL-01
- files: new: docs/delivery/README.md (row format `ID | status | PR | commit | evidence/tests | migration | blocker | next`; status vocabulary; rule: one row per merged packet, appended at merge), new: docs/delivery/S0.md … S5.md (header + empty table), docs/implementation/STATUS.md (top banner only: 'Frozen 2026-09-17 — new rows in docs/delivery/'), docs/implementation/BASELINE.md (top banner only: 'Superseded — the system is not in production; locked facts in docs/agents/domain.md')
- acceptance: seven new files exist; STATUS.md and BASELINE.md differ from main only by the banner lines | PR template (DL-03) links to docs/delivery/README.md
- tests: none (docs)
- risks: none
### DL-16 [S0/S/claude] Seed slice epics S0–S5 with gate checklists and lane/file-ownership tables
- goal: Create the six parent issues that every packet hangs off, so the owner's board shows one card per slice with a live sub-issue progress bar and a concrete gate.
- depends_on: DL-02, DL-03
- files: GitHub: six issues from slice-epic.yml, one per milestone; S1 gate = owner's real failed order visible with all 11 parity sections and per-section status, Import-blocked exceptions visible, reservation→lot→courier booking→dispatch posting→COD remittance→reconciliation→margin demonstrated on the fixture, Playwright parity specs green, audit viewer shows the chain, accountant sign-off on the posting; each epic has a Lanes table (domain → executor → owned paths) and a 'Decisions needed' list, docs/agents/domain.md (link each slice to its epic number)
- acceptance: 6 epics open, each assigned to its milestone and on the project board | S0 epic lists every DL task as a sub-issue after DL-17 runs | gate checklists use the slice definitions verbatim from the brief
- tests: none
- risks: Lane tables need the other domains' final task lists; seed with placeholders and update when DL-17 publishes.
### DL-17 [S0/M/jules] publish-tasks: create/update packet issues from the merged plan JSON
- goal: Turn the merged plan (all domains' task objects in this schema) into GitHub issues idempotently, with labels, milestone, sub-issue link to the slice epic, native blocked_by edges and board membership.
- depends_on: DL-02, DL-03, DL-16
- files: new: scripts/delivery/publish-tasks.mjs (input: JSON file of tasks {id,title,slice,domain,goal,depends_on,executor,size,files,reuse?,acceptance,tests,risks}; idempotency key = `[ID]` prefix in title via `gh issue list --search`; body rendered from docs/agents/task-packet.md; labels exec:/slice:/domain:; milestone by slice; sub-issue: `gh api --method POST repos/sheiko0777/irth-os/issues/<epic>/sub_issues -F sub_issue_id=<db id>` (fallback: 'Part of #<epic>' first line); dependencies: `gh api --method POST repos/sheiko0777/irth-os/issues/<n>/dependencies/blocked_by -F issue_id=<blocker db id>` where db id = `gh api repos/.../issues/<m> --jq .id`; `gh project item-add`; --dry-run; rate-limit friendly (sleep between POSTs)), new: scripts/delivery/__tests__/publish-tasks.test.mjs (pure planner with mocked gh), docs/agents/domain.md (how to run; the plan JSON lives in the scratch dir, not the repo)
- acceptance: `--dry-run` prints the create/update/link plan | real run on the 21 DL tasks creates issues with correct labels/milestone/sub-issue/blocked_by; second run = 0 changes | blocked_by edges visible on the issue page (GitHub dependencies UI)
- tests: scripts/delivery/__tests__/publish-tasks.test.mjs
- risks: Sub-issue/dependency REST endpoints require the 2026 API version header; secondary rate limits on fast POSTs — the doc says to space calls.
### DL-18 [S0/S/claude] Automation security policy for a public repo with label-triggered agents
- goal: Write down and enforce who may pull the triggers (labels, mentions, workflows) and what may never appear in issues/PRs, so prompt injection or a stranger's issue cannot spend agent quota or leak data.
- depends_on: DL-02, DL-06
- files: new: docs/agents/security.md (labels jules/exec:*/schema:approved/claude-code applied only by owner or Claude; every mutating workflow triggered by issues/labels/comments gates `if: github.actor == 'sheiko0777'`; issue/PR text is untrusted data for every agent; no secrets/PII in issues or PRs — fixtures masked per docs/agents/fixtures.md; Jules/Codex environments have no DATABASE_URL, TEST_DATABASE_URL only in CI secrets; `@codex` mentions only by owner; public repo reminder), .github/workflows/schema-guard.yml and any future label-triggered workflow (audit: read-only jobs need no gate; mutating ones must have the actor gate)
- acceptance: docs/agents/security.md exists and is linked from AGENTS.md | `grep -l 'issues:' .github/workflows/*.yml` shows every issue/label-triggered workflow either read-only or actor-gated | secret-scan.yml still runs on every PR
- tests: none
- risks: none
### DL-19 [S0/S/owner] Optional: Codex cloud environment and @codex review as second reviewer
- goal: If the owner wants an independent automated review on external PRs, connect the repo to Codex cloud and enable `@codex review` (or automatic review) — otherwise record the decision not to.
- depends_on: DL-18
- files: Codex cloud UI: repo connected, environment with `corepack enable && CI=true pnpm install --frozen-lockfile`, no DATABASE_URL secret, docs/agents/codex.md (section: cloud review enabled? quota; who may mention @codex)
- acceptance: either `@codex review` posts a review on a probe PR and docs/agents/codex.md documents it, or the decision issue is closed 'not now' with the reason
- tests: probe PR if enabled
- risks: Quota/cost; the owner's tiering rule applies — cloud review is not free.
### DL-20 [S0/S/claude] Fixture policy and masking tool for the real Shopify order (A4)
- goal: Give the OR domain a safe home for the canonical S1 regression fixture: masked copy in the repo, raw copy encrypted outside git, deterministic masking script, secret-scan clean.
- depends_on: —
- files: .gitignore (add `**/__tests__/fixtures/**/*.raw.json` and `**/*.raw.jsonl`), new: docs/agents/fixtures.md (masked path apps/api/src/__tests__/fixtures/shopify/<order-number>.masked.json; raw kept at E:/irth-secure/ encrypted with the owner's key, never in any worktree; masking rules: names → 'Customer NNN', emails → sha1@example.test, phones → +20 with digits replaced keeping length, addresses → street/building replaced, keep all ids/money/currency/timestamps/structure; who may view raw), new: scripts/delivery/mask-fixture.mjs (deterministic, seeded; pure `mask(json)` + CLI in → out), new: scripts/delivery/__tests__/mask-fixture.test.mjs
- acceptance: `git check-ignore apps/api/src/__tests__/fixtures/shopify/x.raw.json` → ignored | mask script output for a sample GraphQL order JSON contains no original name/email/phone/address strings and identical money/ids | TruffleHog (secret-scan.yml) passes on a masked sample committed in a probe branch
- tests: scripts/delivery/__tests__/mask-fixture.test.mjs
- risks: Blocks the OR fixture-capture task; must land before it.
### DL-21 [S0/S/claude] UI-track (astra) integration rules
- goal: Define how the external design track's deliverables enter the same structure so mobile-first UI work runs in parallel without touching data or server code.
- depends_on: DL-02, DL-06, DL-13
- files: new: docs/agents/ui-track.md (label exec:ui; branch ui/<slice>-<id>-<slug>; allowed paths apps/admin/src/app/**, apps/admin/src/components/**, apps/admin/src/styles/**, packages/emails templates; forbidden apps/admin/src/server/**, packages/** except emails; data contract = the data-testid + section-status vocabulary from docs/agents/testing.md (loaded / not_applicable / not_exposed_by_provider / permission_denied / fetch_failed); screenshots (mobile + desktop) required in PR; reviewed via review-external-pr; UI tasks carry UI-nn ids and depend on the OR/DM task that exposes the data), docs/agents/domain.md (UI prefix row links here)
- acceptance: doc exists; schema-guard's exec:ui rule (DL-06) matches the forbidden paths listed here | first UI PR is reviewed with the protocol and its screenshots are in the PR
- tests: none
- risks: UI must not invent data shapes — the section-status vocabulary is owned by OR.

## Open questions
- Which Jules plan is active (Free 15 tasks/day + 3 concurrent, Pro 100/15, Ultra 300/60)? — why: Sets the Jules WIP cap the daily sync enforces and how many S1 jules-lane packets can run in parallel. — default: Assume Free: cap 3 in-flight Jules tasks, prefer batching small packets into one issue only when they share a workspace.
- Should Codex cloud be connected to the repo for `@codex review` / issue-triggered tasks, or stay local via codex-delegate only? — why: Cloud review is an independent second opinion astra implicitly wants, but it costs quota and needs a cloud env with a 10–30 min install baked in. — default: Local only (DL-19 closed 'not now'); Claude review + owner label is the gate.
- Can the owner's Hermes agent push branches / open PRs on sheiko0777/irth-os (does it hold a GitHub token), or does it only produce files? — why: Decides whether exec:hermes tasks end in a PR or in files Claude commits; changes the hermes lane's acceptance wording. — default: Files-to-Claude lane; no direct PRs until the DL-12 probe proves otherwise.
- Will the owner let Claude change repo settings (branch protection, merge methods) with his token, or run the documented gh commands himself? — why: DL-04 is the fix for 'red CI can still auto-merge'; it is blocked until someone with admin runs it. — default: Claude prepares docs/agents/merge-policy.md with exact commands; owner runs them (5 minutes).
- Keep the owner's `claude-code` label as the sole merge trigger for every lane, or allow Claude to auto-merge review:approved PRs from jules/hermes for docs/tests-only changes? — why: Owner throughput on the phone vs. one-place/secure (G10). — default: Owner's label stays the only trigger; docs-only PRs are batched to reduce clicks.
- docs/implementation/BASELINE.md says the system is live with 3 orgs and ~1000 orders/day; the locked owner facts say not in production. Confirm BASELINE is stale. — why: If any tenant data exists, S0/S1 migration tasks need backfills and the 'additive only' rule hardens; DL-15 freezes BASELINE on the stale assumption. — default: Treat BASELINE as stale (owner facts win); freeze it with a banner.
- Does the owner want GitHub Project (v2) fields (Executor/Slice/Status) or are labels + milestones + the Watch artifact enough? — why: The project board needs `gh auth refresh -s project` and one more surface to keep in sync; the Watch artifact already renders the same information. — default: Create the board (cheap, native, phone-friendly) but treat labels/milestones as the source of truth; the board is a view.
- Commit attribution: CLAUDE.md says 'Do not add a Co-Authored-By trailer' while the harness reminder asks for one on Claude commits. — why: Consistency of PR provenance badges on the Watch dashboard (it distinguishes Claude from Jules personas). — default: CLAUDE.md (repo rule) wins; provenance comes from the branch prefix and exec: label.

## Sources checked
- C:/Users/sheri/AppData/Local/Temp/claude/E--CLAUDE/1dc5dbc7-c3fc-4603-87fa-2384c724f8df/scratchpad/codex-architecture-review.md (read in full)
- C:/Users/sheri/.claude/projects/E--CLAUDE/1dc5dbc7-c3fc-4603-87fa-2384c724f8df/tool-results/buqwvyf2l.txt (astra critique, read in full)
- C:/Users/sheri/AppData/Local/Temp/claude/E--CLAUDE/1dc5dbc7-c3fc-4603-87fa-2384c724f8df/scratchpad/synthesis-core.json (read in full)
- C:/Users/sheri/AppData/Local/Temp/claude/E--CLAUDE/1dc5dbc7-c3fc-4603-87fa-2384c724f8df/scratchpad/agent-summaries.json (read in full)
- C:/Users/sheri/AppData/Local/Temp/claude/E--CLAUDE/1dc5dbc7-c3fc-4603-87fa-2384c724f8df/scratchpad/codex-astra-brief.md (read in full)
- CLAUDE.md, AGENTS.md, package.json, turbo.json, .mcp.json, .claude/settings.json, .claude/launch.json
- .github/workflows/{ci,claude-code-automerge,secret-scan,deploy-api,deploy-all}.yml (deploy-admin.yml exists, not read)
- .claude/agents/{money-reviewer,tenancy-reviewer}.md; .claude/hooks/{money-gate,protect-paths}.mjs; .claude/skills/{new-migration,verify-gate}/SKILL.md
- .jules/{bolt,sentinel}.md; docs/implementation/{BASELINE,STATUS}.md (STATUS skimmed: header + F01/F02/F11 rows); packages/db/scripts/migrate.mjs (header); packages/db/drizzle listing (0053–0066); apps/admin/src/__tests__/tenancyGate.test.ts, integration/{rlsCoverage,schemaDrift}.test.ts (headers); apps/api/src/__tests__ listing; kernel file existence check (ledger.ts, costing.ts, schema/ledger.ts, idempotency.ts, outbox.ts, orgContext.ts, permissions.ts, index.ts, webhooks/shopify.ts, outboxWorker.ts)
- docs/agents/ — verified absent in E:/irth-os-jules-land and E:/irth-os; .github/ISSUE_TEMPLATE, PULL_REQUEST_TEMPLATE.md, CODEOWNERS — absent; apps/admin/playwright.config.ts — absent
- git: worktree list, branch -a, log (both worktrees); gh: repo view (public, merge methods), label list, milestones (none), issue list, pr list (40 recent incl. Sentinel/Bolt), branch protection (required_status_checks null, 0 reviews), rulesets (none), actions permissions, project list (missing read:project scope)
- E:/CLAUDE/jules-phase26-prompt.txt (existing Jules prompt style); E:/CLAUDE/jules-irth-os-* directories
- Memory: C:/Users/sheri/.claude/projects/E--CLAUDE/memory/{project_irth_os_multi_agent_lead,feedback_codex_model_tiering,project_irth_os_pnpm_hazard}.md and MEMORY.md index (Watch dashboard artifact + write_db refresh + session-only cron)
- Skills read in full: codex-delegate/SKILL.md + references/writing-the-brief.md; delegate-setup; claude-devfleet; hermes-imports; gh-issues; github-project-management; fetch-github-issues; configure-issue-tracker; brainstorm-to-issue; superpowers-issue-bridge; superpowers 6.1.1 writing-plans, executing-plans, subagent-driven-development; mattpocock to-tickets + setup issue-tracker-github.md; ecc 2.2.1 commands epic-{decompose,publish,sync,claim,validate,review}.md + scripts/github-coordination.js + lib/github-coordination/{policy,parsing}.js; commands/github/issue-tracker.md; kimi/grok/opencode/agy/claude-delegate (front matter + dispatch lines)
- Installed CLIs: codex-cli 0.153.4, gh 2.97.0, opencode; jules CLI not installed (npm global)
- https://jules.google/docs (getting started); https://jules.google/docs/usage-limits/ (Free 15/3, Pro 100/15, Ultra 300/60); https://jules.google/docs/running-tasks/ (label `jules`, plan approval, Jules comments + PR link, branch owned by user); https://jules.google/docs/cli/reference/ (`jules remote new --repo --session --parallel`, `remote pull`); https://github.com/google-labs-code/jules-action (inputs prompt/jules_api_key/starting_branch; issue-trigger security warning); web search results: Jules API POST /v1alpha/sessions with automationMode AUTO_CREATE_PR, npm @google/jules
- https://learn.chatgpt.com/docs/cloud (redirect target of developers.openai.com/codex/cloud: @codex on PRs/issues, environments, AGENTS.md); https://learn.chatgpt.com/docs/third-party/github (`@codex review`, automatic PR review, requires Codex cloud set up); https://learn.chatgpt.com/docs/non-interactive-mode (`codex exec` --json/-o/--sandbox/--output-schema, resume)
- https://hermes-agent.nousresearch.com/docs/ (Nous hermes-agent: CLI/desktop, gateways, cron, skills); https://hermes-agent.ai/integrations/github (webhook/cron GitHub workflows, triage, PR review summaries, Actions failure analysis, PR creation demo, start read-only)
- https://docs.github.com/en/rest/issues/issue-dependencies (GET/POST/DELETE /repos/{o}/{r}/issues/{n}/dependencies/blocked_by with issue_id)