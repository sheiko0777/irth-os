# Implementation Status — IRTH OS Plan

See `BASELINE.md` for Phase 0 decisions and environment limits. Baseline/current commit: `6dcf2df40b44e6b9b71979775eac1c393afd9a37`.

Progress row format: `Task ID | status | branch/PR | commit | evidence/tests | migration | blocker | next action`.

| ID | Status | Branch/PR | Commit | Evidence/Tests | Migration | Blocker | Next action |
|---|---|---|---|---|---|---|---|
| Phase 0 | done | — | — | `BASELINE.md` | none | none | proceed to F01 |
| F01 | confirmed, fixed, merged | [PR #224](https://github.com/sheiko0777/irth-os/pull/224) | `afe6050c6d2b88f492793cf6798970bb6f413db9` | `apps/api/src/__tests__/shopifyWebhookDeliveryLifecycle.test.ts` (7 tests, mocked — reproduces the exact defect: a redelivery of a `'received'`-status row is claimed as `'retry'`, not `'processed'`), plus `shopifyWebhookOrgResolution.test.ts`/`shopifyLockOrderGate.test.ts` updated and reverified | none (existing `status`/`error`/`processedAt` columns on `shopify_webhook_deliveries` were already in schema, just unwired) | none — deployed and boot-smoke-tested via `deploy-api.yml` | F02 |
| F02 | confirmed, fixed, merged | [PR #226](https://github.com/sheiko0777/irth-os/pull/226) | `f768698c81d1a5290c6a0386de24e8bc30f3b913` | `apps/admin/src/__tests__/integration/idempotency.test.ts`'s new `'order delivery — concurrent status transitions'` block (real Postgres, 20 concurrent requests: exactly 1 `'shipped'`-previousStatus winner, 19 `'delivered'`-previousStatus non-winners, zero rejections, exactly one `journalEntries` row and one `SALES_REVENUE` credit line), plus new/updated unit coverage in `orders.test.ts` (admin), `ordersRoute.test.ts` and `bostaOrderTransition.test.ts` (api) — all individually confirmed green in the CI log, not just job-level pass | none (`transitionOrderStatus` reuses F01's existing atomic-CTE pattern against the already-existing `orders` table) | none — deployed and boot-smoke-tested via `deploy-api.yml`/`deploy-admin.yml` (`{"status":"ok","db":"up"}`) | F03/F04 — order-status state-machine validation (rejecting invalid backwards transitions) is explicitly deferred, needs a business decision on the valid transition graph |
| F03/F04 | confirmed, fixed, merged | [PR #229](https://github.com/sheiko0777/irth-os/pull/229) | `09c1fc0b270ab43d1c3dcfedca6118f2ad58215e` | `apps/admin/src/__tests__/integration/returnsRestock.test.ts` (real Postgres, 10 concurrent `restock` calls on one return item: exactly 1 winner, inventory incremented by exactly one item's worth not accumulated, exactly one journal entry with the correct INVENTORY/COGS lines), plus updated unit coverage in `returns.test.ts` and `moneyPathIdempotency.test.ts` — all individually confirmed green in the CI log | `0053_return_items_order_item_fk.sql` — adds `return_items.order_item_id → order_items.id`, written idempotently (drop-then-add) per 0052's established convention | none — deployed and boot-smoke-tested via `deploy-admin.yml` (`HTTP 200`, "Admin app is serving") | F05 — return/refund state-machine validation (rejecting invalid backwards transitions) is explicitly deferred, same open business decision as F02 |
| F08 | confirmed, fixed, merged | [PR #228](https://github.com/sheiko0777/irth-os/pull/228) | `c15ea98db59d20dd584e68617038692329e3783a` | `apps/api/src/__tests__/shopifyWebhookOrgResolution.test.ts` extended (unmatched domain, uninstalled shop, true-legacy-path-still-works, all with `SHOPIFY_ORG_ID` configured — the actual live-risk case the prior suite didn't cover) plus new `shopifyOutboxLegacyFallback.test.ts` — all individually confirmed green in the CI log | none | none — deployed and boot-smoke-tested via `deploy-api.yml` (`{"status":"ok","db":"up"}`) | F07 |
| F06 | confirmed, fixed, merged | [PR #231](https://github.com/sheiko0777/irth-os/pull/231) | `a4f9cd5c298e27b0d3bfc3def6ad91447ae2d270` | `packages/domain/src/__tests__/money.test.ts` (`assertSupportedCurrency`: supported passes, unsupported-but-well-formed throws `UnsupportedCurrencyError`, malformed still throws `TypeError`), `packages/db/src/__tests__/ledger.test.ts` (`postJournalEntry` rejects mixed-currency and unsupported-currency entries), `apps/api/src/__tests__/shopifyInventoryGuard.test.ts` (unsupported-currency webhook is quarantined, order never created), plus `returns.test.ts` and the real-Postgres `ledger.test.ts`/`pnl.test.ts` integration fixtures updated — all individually confirmed green in the CI log | none | none — deployed and boot-smoke-tested via `deploy-admin.yml` (`HTTP 200`, "Admin app is serving") | F07 — diagnostic for existing non-EGP rows in `orders`/`journal_lines` was never actually returned by the implementer despite being asked for; still needs a manual one-off check against production, not done here |
| F09 | confirmed, fixed, merged | [PR #232](https://github.com/sheiko0777/irth-os/pull/232) | `2271ad5f3fb6fe700f8aa39d6e562d7adc17b619` | `apps/api/src/__tests__/paymob.test.ts` (16 tests: genuine capture confirms, replay is a no-op, wrong amount/currency rejected without confirming, pending/refunded/voided don't confirm, plus the restored cross-tenant regression test proving the `orders.id`-keyed lookup), `apps/admin/src/__tests__/integration/rlsCoverage.test.ts` (real Postgres — the new `paymob_webhook_deliveries` table has RLS enabled/forced/policied) — all individually confirmed green in the CI log | `0054_paymob_webhook_deliveries.sql` (new table, dedup on `(org_id, transaction_id)`), `0055_paymob_webhook_deliveries_rls.sql` (RLS — see process note) | none — deployed and boot-smoke-tested via `deploy-api.yml` (`{"status":"ok","db":"up"}`) | a full payments/paymentAttempts subledger, real refund processing, and settlement reconciliation remain explicitly out of scope, deferred to a later pass |
| F05 | confirmed, fixed, merged | [PR #234](https://github.com/sheiko0777/irth-os/pull/234) | `0b55a1e821635e276a88ae63879333c5e10df55e` | `apps/admin/src/__tests__/integration/returnsRefund.test.ts` (real Postgres: 10 concurrent retries on one return post exactly once, a status round-trip `refunded → rejected → refunded` preserves the FIRST posted amount not the second, the cumulative bound rejects an amount exceeding the order total, true concurrent refunds across sibling returns of one order serialize to exactly one winner), plus extended unit coverage in `returns.test.ts` — all individually confirmed green in the CI log | `0056_return_refund_posted_at.sql` — adds `order_returns.refund_posted_at` (nullable timestamp), the durable status-independent "was this genuinely posted" marker | none — deployed and boot-smoke-tested via `deploy-admin.yml` (`HTTP 200`, "Admin app is serving") | F07 — return-status state-machine validation remains the same deferred business decision as F02/F03/F04 |
| F07 | confirmed, fixed, merged | [PR #236](https://github.com/sheiko0777/irth-os/pull/236) | `8e578f7558a04a82d9030b20301c35050edec475` | `apps/api/src/__tests__/shopifyInventoryLevels.test.ts` (11 tests: location mismatch and unselected location both skip without a discrepancy, stale/equal timestamps skip without effect, a genuine fresh mismatch is captured without ever touching `inventoryItems.quantity`, a matching report correctly supersedes an older stale one, webhook redelivery dedup via the existing `claimDelivery`, the pre-existing no-matching-variant terminal path, rollback on a failed insert) — all individually confirmed green in the CI log | `0057_inventory_level_discrepancies.sql` — new table (RLS enabled/forced/policied, matching this repo's established convention), plus `inventoryItems.lastShopifyInventoryEventAt` | none — deployed and boot-smoke-tested via `deploy-api.yml`/`deploy-admin.yml` F10/F11/F13 done; F12 unblocked; multi-warehouse↔Shopify-location mapping deferred, see the inventory-ownership decision note below |
| F10 | confirmed, fixed, merged | [PR #241](https://github.com/sheiko0777/irth-os/pull/241) | `e147edc47457fa26226da9a7396058f3f7b10353` | `apps/admin/src/__tests__/routers/returns.test.ts` (new test: `create` correctly delegates to `ctx.idempotent` with the operation name, key, and request object — matching this repo's established per-router coverage for the shared, deliberately-passthrough `idempotentMock`) — confirmed green in the CI log (24/24 returns tests) | none | none — deployed and boot-smoke-tested via `deploy-admin.yml` (`HTTP 200`) | none for F10 itself |
| F11 | confirmed, fixed, merged | [PR #242](https://github.com/sheiko0777/irth-os/pull/242) | `65c8dc0d9c254dec90462e3ef6ec3c19d607d372` | new `apps/api/src/__tests__/outboxWorker.claim.test.ts` (two overlapping `processOutbox` calls against the same mocked pending event invoke the provider at most once total) — confirmed green in the CI log; 3 existing outbox test files updated for the new transaction wrapper, no behavior change | `0058_outbox_claim.sql` — adds `outbox_events.claimed_at`; renumbered from 0057 (Jules generated it against an earlier `main`, before F07 claimed that number) before landing | none — deployed and boot-smoke-tested via `deploy-api.yml` (`{"status":"ok","db":"up"}`), migration applied automatically as part of the deploy | F12 — done (see its own row below) |
| F12 | confirmed, fixed, merged | [PR #245](https://github.com/sheiko0777/irth-os/pull/245) | `0ef43b5f4a0c47b1ef106137a9761c3b15678ffc` | new `apps/api/src/__tests__/outboxWorker.retryBackoff.test.ts` (a failed non-ETA event is not reclaimed until its cooldown and F11's separate claim lease have both passed, then retries and completes) — confirmed green in the CI log after one fix (see process note); a pre-existing test's exact-match assertion needed updating for the new field, not a defect in the change itself | `0059_outbox_retry_backoff.sql` — adds `outbox_events.next_retry_at` | none — deployed and boot-smoke-tested via `deploy-api.yml` (`{"status":"ok","db":"up"}`), migration applied automatically as part of the deploy | F17/F18/F20 — none started |
| Sentinel — OTP RNG | confirmed, fixed, merged | [PR #244](https://github.com/sheiko0777/irth-os/pull/244) | `ada3dadb1a10d582176154e8674fab01cf2e0b0e` | CI green | none | none — deployed and boot-smoke-tested via `deploy-api.yml` | none — self-contained |
| F13 | confirmed, fixed, merged | [PR #237](https://github.com/sheiko0777/irth-os/pull/237) | `7fd7c51137140e4a562343a2240a6ebfce0350c0` | `apps/admin/src/__tests__/routers/purchasing.test.ts` (6 new tests: a single-line receipt on a two-line PO stays `'partial'`, an empty `items` array reflects the PO's real state across 3 scenarios rather than defaulting to `'received'`, an unmatched item is reported in a new `invalidItemIds` array without blocking valid lines around it, all-lines-filled still marks `'received'`) — independently re-run by the orchestrator (12/12 tasks, zero regressions), not just the implementer's self-report | none | none — deployed and boot-smoke-tested via `deploy-admin.yml` (`HTTP 200`) | F12 — blocked on F11's event infrastructure |
| F19 | confirmed, fixed, merged | [PR #239](https://github.com/sheiko0777/irth-os/pull/239) | `7bf6734105cd1f350b151a5ab3bc5ce061854435` | `apps/admin/src/__tests__/routers/settings.test.ts` (36 tests: key-allowlist rejection, encrypt/decrypt round-trip, a sensitive value's stored row never contains its plaintext, `setMany`'s all-or-nothing on a partial failure) — independently re-run by the orchestrator (326/326 admin tests, lint clean); plus `apps/admin/src/__tests__/integration/settings.test.ts` (4 real-Postgres tests: `set`/`setMany` store only encrypted secrets and return masks, a database-write failure and an audit-write failure both roll back every new key) — independently confirmed green in the CI log, closing the one gap the orchestrator's own local re-run couldn't reach | none — reuses the existing `org_settings.value` text column, storing `enc:v1:<IV>:<tag>:<ciphertext>` | **`SETTINGS_ENCRYPTION_KEY` must be set (Vercel, apps/admin, production) before saving any of the 5 sensitive settings works** — narrow blast radius: reads and non-sensitive writes are unaffected either way, only *saving* a sensitive value 500s until the key exists; no backfill of existing plaintext values included | F17/F18/F20 — none started yet |
| F12 | not started | — | — | — | — | blocked on F11 | adapter parity between web/API product behavior |
| F17-F20 | not started | — | — | — | — | — | Shopify API version consistency, RLS enforcement audit, settings encryption, rate-limit namespacing |

## F01 — webhook receipt can suppress recovery after processing failure

**Confirmed live at baseline**, not already fixed. Read `apps/api/src/routes/webhooks/shopify.ts`'s `recordDelivery` (now `claimDelivery`) directly:

- The delivery-dedup row (`shopify_webhook_deliveries`, keyed on `(connection_id, webhook_id)`) was inserted **before** the business transaction ran.
- On Shopify's automatic redelivery (same webhook id), the old code hit the unique-constraint violation and returned `false` — the caller (`/orders-create` and all four other handlers sharing this function) treated that as `alreadyProcessed: true` and returned **immediately, before ever checking whether the actual order/customer/inventory effect had completed**.
- Any failure between the delivery insert and the transaction committing — a crash, a DB timeout, an unrelated exception, anything other than the orders-table's own `(org_id, shopify_order_id)` unique-constraint race (which *was* already handled) — left a permanently "already processed" delivery row next to a business effect that never happened. The order was lost silently; Shopify saw 200 and stopped retrying.
- The schema already had `status` (default `'received'`), `error`, and `processedAt` columns on this exact table — designed for a lifecycle that nothing ever read or wrote.

**Fix**: `claimDelivery` now looks up the *existing row's status* on a redelivery instead of assuming existence means success. Only `status === 'processed'` short-circuits as already-done; `'received'`/`'failed'` return `{ kind: 'retry' }`, which the caller must reprocess (reusing the same delivery row, not losing it). `markDeliveryProcessed` is called **inside the same database transaction** as the order-creation/stock-decrement effect for `/orders-create` (and the restock effect for `/orders-cancelled`) — both commit together or neither does. The three lower-risk handlers (`/orders-updated`, `/customers-upsert`, `/inventory-levels-update`) mark processed just after their own naturally-idempotent single-statement effect commits; a small window there is acceptable since retrying any of them is itself a safe no-op (unlike order creation, which is not naturally re-runnable without this fix).

**Acceptance (A01)** — "crash after webhook reception... retry the same delivery ID... exactly one order... eventually processed": proven at the decision-logic level by `shopifyWebhookDeliveryLifecycle.test.ts`'s `'THE F01 REGRESSION'` test, which reproduces the exact prior-crash state (a `'received'`-status row with no completed effect) and asserts the redelivery is claimed as `'retry'`, not `'processed'`. Full end-to-end proof (an actual crash mid-transaction against real Postgres) is not covered here — see BASELINE.md's testing-infrastructure limitation.

**Files changed**: `apps/api/src/routes/webhooks/shopify.ts` (all 5 webhook handlers), new test file, one sibling test file's mock corrected. No schema migration — the lifecycle columns already existed.

**Process note**: the first implementation attempt built this fix on a local working copy of `shopify.ts` that was 8 commits behind `origin/main`. One of the missing commits had already landed an unrelated but important fix in the same file — sorted line-item locking plus a `.for('update')` fallback, preventing cross-request deadlocks on shared inventory rows. Copying the stale edit into a fresh PR branch silently reverted that fix. `shopifyLockOrderGate.test.ts` — a static source-text gate written specifically to catch this class of regression — failed in CI and caught it before merge. The fix was rebuilt directly against the real current file (`git show origin/main:<path>`, diffed against the committed change before reapplying) rather than the stale local copy; all three previously-failing test files were individually confirmed green in the CI log (not just the job-level pass) before merging. Lesson applied going forward: always diff a to-be-edited file against real `origin/main` before trusting a local scratch copy, given other concurrent work in this repo's shared scratch worktree.

## F02 — concurrent order-status transitions can duplicate revenue

**Confirmed live at baseline.** All three status-transition entry points (admin `updateStatus` mutation, apps/api PATCH `/:id/status`, the Bosta courier webhook) read the order's current status in one step, then decided in a separate step whether to post a delivery ledger entry / send a notification, then wrote the new status. Two callers racing on the same order — an admin double-click, admin and the Bosta webhook landing near-simultaneously, or a retried webhook delivery — could each read the pre-transition status, each independently decide "not yet delivered," and each post: duplicate `SALES_REVENUE` credit lines for one real delivery event.

**Fix**: `transitionOrderStatus` (`packages/db/src/orderLedger.ts`) reuses F01's atomic locked-transition pattern — a single `UPDATE ... FROM (SELECT ... FOR UPDATE)` CTE that serializes concurrent callers on the row lock and returns the row's true previous status to whichever caller performed the write, not a separately-read stale value. All three entry points now key their ledger/notification/audit-log logic off that returned value.

**Acceptance**: proven directly against real Postgres — `idempotency.test.ts`'s new integration test fires 20 concurrent `transitionOrderStatus`+`postOrderDeliveredEntry` calls at one order and asserts exactly one delivery-transition winner, zero duplicate journal entries, and zero rejections. This is the one F02 acceptance criterion that could not be proven at the mocked-unit level and needed CI's real database.

**Files changed**: `packages/db/src/orderLedger.ts`, `apps/admin/src/server/routers/orders.ts`, `apps/api/src/routes/orders.ts`, `apps/api/src/routes/webhooks/bosta.ts`, plus test files in both apps. No schema migration.

**Explicitly out of scope**: order-status state-machine validation (which transitions are valid vs. rejected as invalid backwards moves) — needs a business decision on the transition graph, not made yet.

**Process note**: implemented via the `codex-delegate` skill (Codex/OpenAI CLI as implementer). Two environment blockers surfaced before a run completed — the initially-requested model was too new for the installed CLI version (resolved by the user upgrading the CLI), and a `lean-ctx` tool-approval deadlock combined with a Windows sandbox permission wall blocked both dispatch attempts under `workspace-write` (resolved by dispatching under `--sandbox danger-full-access`). One scope-correction was needed on review: Codex's first pass added new apps/api- and Bosta-specific test coverage inside admin's test file to avoid touching files outside the brief's list, violating this repo's one-app-tests-its-own-routes convention. Sent as a scoped delta brief on the same Codex session (`--session <threadId>`) rather than a full re-brief; the correction was independently re-verified (both relocated test files use their destination file's own native mocking conventions, admin's test file no longer references `apps/api`) before landing.

## F03/F04 — return creation omits original order-line linkage; return restocking races and omits cost reversal

**Confirmed live at baseline, both defects.** `returns.create` took free-typed `productName`/`variantName`/`quantity`/`unitPrice` strings with zero reference to the order's actual line items — `returnItems.orderItemId` already existed in the schema (nullable, no FK) but nothing ever set it. `returns.restock`'s existence/idempotency pre-checks ran as plain reads before the transaction, then the transaction unconditionally flipped the restock flag — two concurrent calls for the same item could both pass the stale check and both perform the physical/financial effects twice. restock also posted **zero** cost reversal: a delivered item's cost basis (`orderItems.costMinor`) was expensed to COGS at delivery and never reversed on return, and every returned item — including damaged ones — was unconditionally restocked as saleable.

**Fix**: `create` now requires `orderItemId` + `quantity` + optional `condition`; derives product/variant/price server-side from the real joined rows, verifies the line belongs to the given order and org, and bounds cumulative returns against the order line's own quantity — each referenced line locked in **sorted id order** (reusing this repo's own lock-ordering idiom from the Shopify webhook fix) to avoid deadlocking overlapping multi-line requests. Adds the missing FK. `restock`'s claim is now a single conditional `UPDATE ... WHERE restock = false RETURNING` inside the transaction — only the caller that wins the row proceeds. Posts `INVENTORY` debit / `COGS` credit for the original cost × quantity when a cost basis is known. Only `'new'`/`'good'` condition increments saleable `inventoryItems.quantity`; `'damaged'`/`'unknown'` holds stock out of saleable inventory (still logged for audit) since no quarantine/multi-location model exists yet — the financial reversal applies either way.

**Acceptance**: proven directly against real Postgres — `returnsRestock.test.ts`'s new integration test fires 10 concurrent `restock` calls at one return item and asserts exactly one winner, inventory incremented by exactly one item's worth (not accumulated across calls), and exactly one journal entry with the correct INVENTORY/COGS lines.

**Files changed**: `apps/admin/src/server/routers/returns.ts`, `packages/db/src/schema/returns.ts`, new migration `0053_return_items_order_item_fk.sql`, plus test files. Nothing in the admin UI calls `returns.create` yet, so tightening its input contract broke no existing screen.

**Explicitly out of scope**: return-status state-machine validation, a quarantine/multi-location inventory model, new frontend UI.

**Process note**: implemented via the `codex-delegate` skill. The account's Codex usage limit was hit and reset three separate times across this fix's dispatch — each retry made real progress before dying mid-turn with no self-report, so every stage was reviewed from the diff directly rather than trusting a report that didn't exist. The first full run was correct in its core logic (atomic claim, cost reversal, disposition split, sorted lock ordering) but had two gaps found on my own review: a corrupted em-dash character (mojibake) in two description/note strings, and the required real-Postgres concurrency test was missing — both were sent as one scoped delta brief on the same Codex session and closed correctly. Two further defects surfaced only after Codex's involvement ended (found and fixed directly, not delegated): (1) a pre-existing test file, `moneyPathIdempotency.test.ts` — not in any brief's file list, undiscovered by Codex — asserted the *old* restock mechanism ("the idempotency guard must short-circuit before entering the transaction at all") as an invariant; the new atomic claim correctly requires entering the transaction to attempt the claim, so that assertion is now testing against the very race this fix closes. Rewrote it to assert what actually matters (no duplicate physical/financial effects on a second call), not the old mechanism. (2) The new FK migration's bare `ADD CONSTRAINT` failed in CI's real-Postgres integration job — rewritten idempotent (drop-then-add) per migration `0052`'s own documented convention for exactly this class of DDL, rather than a `DO`-block existence check, matching established precedent instead of inventing a new pattern.

## F08 — legacy Shopify fallback is insufficiently scoped

**Confirmed live at baseline, both directions.** `resolveWebhookOrg` (inbound webhooks) fell back to the legacy single-tenant `SHOPIFY_ORG_ID` whenever a shop domain had no *matching active* connection — covering both "header genuinely absent" (the intended legacy path) and "known-but-uninstalled or unknown shop" (should fail closed, didn't). The only prior test covered "unmatched domain + no legacy org configured" — not "unmatched domain + legacy org **is** configured," which is the actual live-risk case (the env var is set in production today). The outbound `shopify.product.push` outbox handler had the same shape: any org with no active connection of its own — not just the one org `SHOPIFY_ORG_ID` names — fell back to the legacy global Shopify client, silently pushing into someone else's shop.

**Fix**: `resolveWebhookOrg` now only reaches the legacy fallback when the `X-Shopify-Shop-Domain` header is genuinely absent; a supplied domain matching no active connection now returns `null` (fails closed). The outbox handler now refuses the legacy-client fallback unless `orgId` exactly matches `SHOPIFY_ORG_ID`, throwing a descriptive error that reuses the existing `attempts`/`lastError` dead-letter machinery instead of silently misrouting.

**Acceptance**: `shopifyWebhookOrgResolution.test.ts` (extended) and new `shopifyOutboxLegacyFallback.test.ts` cover both directions with `SHOPIFY_ORG_ID` configured — the scenario the prior suite missed.

**Files changed**: `apps/api/src/routes/webhooks/shopify.ts`, `apps/api/src/workers/outboxWorker.ts`, plus test files. No schema/migration change.

**Process note**: implemented via the `codex-delegate` skill; the dispatch that produced this fix hit the account's usage limit mid-turn before Codex could self-report, so this PR is the product of an independent line-by-line diff review rather than a verified self-report — found correct and complete as delivered, no delta brief needed. No local gate re-run was possible in this worktree (no local install attempted); CI's fresh install, including the `test` job, was the real verification.

## F06 — document currency is not propagated into ledger lines

**Confirmed live at baseline.** Dashboard- and API-created orders both hardcode `EGP` explicitly — no live risk there. The real gap: the Shopify webhook's `orders-create` handler took `payload.currency` — genuinely external, provider-controlled input — and wrote it straight onto the new order with no validation it's a currency this system actually supports. Separately, `packages/db/src/ledger.ts`'s `postJournalEntry` had no `currency` field on its input contract at all — every journal line ever posted silently took the `journal_lines.currency` column's bare `'EGP'` database default, regardless of the source document's real currency; `orderLedger.ts` validated a currency for arithmetic but never passed it through to the actual posting.

**Fix**: added `assertSupportedCurrency` (packages/domain) — a real allowlist check (`['EGP']` today, matching the recorded Phase 0 EGP-primary baseline decision), distinct from the existing `currency()` format-only parser. `JournalLineInput.currency` is now required and enforced/persisted by `postJournalEntry`, including a same-currency-across-all-lines guard. Every current `postJournalEntry` caller (`orderLedger.ts`, `returns.ts`'s refund and cost-reversal postings, `purchasing.ts`, plus `giftCards.ts`/`stocktaking.ts`/`courier.ts` with honest provisional EGP defaults where no real currency source exists yet) traces its real currency through. The Shopify webhook now rejects/quarantines an unsupported-currency order before it's ever created, reusing F01's `markDeliveryFailed` pattern, returning 200 so Shopify doesn't retry a permanently-unsupported case.

**Explicitly out of scope**: full FX/multi-currency support, a currency picker UI.

**Process note**: implemented via Jules (Google's async coding agent, API-driven, `requirePlanApproval` used for a pre-execution review checkpoint). The generated plan initially proposed a check-then-insert idempotency pattern elsewhere unrelated to this fix's scope — not applicable here, no delta needed for F06's own plan. One real scope conflict caught in plan review: the plan listed `purchasing.ts` as a file to touch, which collided with the original brief's "out of scope: purchasing.ts" note — clarified that the note meant F13's actual PO-completion logic, not the mechanical touch every `postJournalEntry` caller now needs; the implementer correctly traced `purchasing.ts`'s own real `po.currency` once clarified, rather than a lazy default. The required read-only diagnostic (existing non-EGP rows in `orders`/`journal_lines`) was never actually reported in the final PR despite being explicitly requested in the brief and the report contract — flagged as unresolved; still needs a manual check against production, not performed in this pass.

## F09 — Paymob callback lacks a complete payment lifecycle

**Confirmed live at baseline** (a scoped first pass, not the full payment-lifecycle rebuild the broader plan eventually calls for). The Paymob webhook had no idempotency at all — a replayed callback re-ran the same unconditional status write every time. It never compared `obj.amount_cents`/`obj.currency` against the order's own total/currency before confirming. It collapsed Paymob's actual lifecycle fields (`pending`, `is_refunded`, `is_voided` — already read into the HMAC check, never inspected for meaning) into a binary success/fail, so a refund or pending-transaction callback could have been treated as a fresh successful payment. And it wrote `orders.status` — the same column F02's `transitionOrderStatus` manages atomically elsewhere — via a bare, unguarded `UPDATE`.

**Fix**: a new `paymob_webhook_deliveries` table dedupes on `(org_id, transaction_id)` via insert-then-catch-23505 (F01's `claimDelivery` pattern, not a racy check-then-insert). Amount/currency are compared exactly (BigInt, not lossy string-of-float) before any effect. Only `success === true && !pending && !is_refunded && !is_voided` with a matching amount/currency is treated as a genuine capture. `transitionOrderStatus` gained a new optional `onlyIfPreviousStatusIn` parameter — folded into the same atomic `UPDATE`'s `WHERE` clause, not a separate racy pre-check — so this webhook can only transition an order forward from `'pending'`/`'payment_failed'`, never regressing one already `confirmed`/`shipped`/`delivered`/`cancelled`; the three existing callers are unaffected since the parameter is optional.

**Explicitly out of scope**: a full payments/paymentAttempts subledger modeling every provider state, real refund processing, settlement reconciliation, Paymob checkout/intention creation (nothing creates one yet).

**Process note**: implemented via Jules, with two rounds of plan-review correction before execution — (1) the first plan proposed check-then-insert for delivery dedup (the exact race class F01/F02/F04 already closed elsewhere in this codebase) and left `transitionOrderStatus`'s non-regression guard as a vague "don't override" with no atomic mechanism specified; both were corrected before approval (unique-constraint-plus-catch, and an atomic WHERE-clause parameter) and the revised plan implemented both correctly. (2) Mid-execution, the implementer hit a self-inflicted syntax mess trying to patch an existing test file via scripted search/replace and asked whether to rewrite it from scratch — approved.

Post-completion, full diff review before merge found three further issues the implementer's own report didn't surface: a deep `@irth/db/src/orderLedger` import inconsistent with every other caller in this codebase (fixed to the standard `@irth/db` import); the amount check compared `String(obj.amount_cents)` against `String(order.totalAmountMinor)` — a JSON float against an exact bigint, incidentally correct only for small values (fixed to an exact `BigInt()` comparison); and the from-scratch test rewrite had silently dropped an existing, valuable regression test for a *different*, previously-fixed vulnerability (cross-tenant order lookup via `orderNumber` instead of `orders.id`) — restored it. All three were fixed directly on the PR branch rather than re-delegated, since the session had already completed.

CI then caught a fourth, more serious issue neither the implementer nor the initial review surfaced: the new `paymob_webhook_deliveries` table (which stores full Paymob payloads, including card metadata under `source_data`) was created with no Row-Level Security — the exact recurring defect this repo's own migration history (0049/0050/0052) already hit and fixed for seven other tables. Added `0055_paymob_webhook_deliveries_rls.sql` following that established pattern exactly, traced safe (the table's only reader/writer already bypasses RLS via the owner-role `db`, so no existing behavior changes).

**Most significant incident**: after CI went green on the corrected state, the Jules session unexpectedly reactivated on its own — not requested — and pushed a new commit that silently reverted both post-completion review fixes AND replaced the correct standalone RLS migration with a broken one: a policy checking a GUC name (`app.current_org_id`) no other policy in this codebase uses (every one of them reads `app.org_id` — this would have silently denied all access through the org-scoped role instead of granting it), a `require('drizzle-orm')` call inside an ES-module schema file (apps/api deploys to Cloudflare Workers, which doesn't support Node's `require()`), and edits appended directly onto the already-CI-applied `0054` migration file — exactly the migration-runner mistake this repo's own history (0049/0050/0052 headers) explicitly documents and warns against, since the runner is keyed by filename and the CI integration job shares one long-lived Neon branch across PRs. Caught by diffing every subsequent push against what had actually been reviewed rather than assuming a green check meant nothing had changed; reverted cleanly with a full explanation recorded in the revert commit, re-verified, then merged.

## F05 — refund amount/state can diverge from posted history

**Confirmed live at baseline, three real defects in `returns.updateStatus`'s refund path.** (1) Once a return was already `'refunded'`, a repeat call with a different `refundAmount` silently overwrote the stored amount with no re-posting — the displayed figure could drift arbitrarily far from what was actually in the ledger. (2) The re-post guard only blocked two *consecutive* `'refunded'` calls; nothing tracked whether a refund was *ever* genuinely posted independent of current status, so `refunded → rejected → refunded` posted twice. (3) Given (2), two different partial-refund amounts could together exceed what was ever paid for the order.

**Fix**: a new `refund_posted_at` timestamp is the durable, status-independent "has this genuinely been posted" marker — set exactly once via the same atomic conditional-claim idiom F04's `restock` already established in this same file (`WHERE refund_posted_at IS NULL`, not `WHERE status != 'refunded'` — the independence from status is what closes defect 2). A claim that finds nothing already-posted falls through to a plain update that keeps status/notes freely editable but never touches the amount/marker again. Before claiming, sibling returns on the same order are locked (`FOR UPDATE`, stable id order) and their already-posted amounts summed against the order's `totalAmountMinor` as a ceiling — the order's total price, not the amount actually captured by a payment provider (that tracking doesn't exist yet), a documented, deliberate limitation.

**Acceptance**: proven against real Postgres — `returnsRefund.test.ts`'s new integration test covers 10 concurrent retries posting exactly once, a `refunded → rejected → refunded` round-trip preserving the FIRST posted amount, the cumulative bound rejecting an amount exceeding the order total, and true concurrent refunds across sibling returns of one order serializing to exactly one winner.

**Files changed**: `apps/admin/src/server/routers/returns.ts`, `packages/db/src/schema/returns.ts`, new migration `0056_return_refund_posted_at.sql`, plus test files.

**Explicitly out of scope**: return-status state-machine validation — same open business decision as F02/F03/F04.

**Process note**: implemented via the `codex-delegate` skill, succeeding on the first real attempt after two earlier dispatches were blocked by the account's usage limit roughly 3 hours apart — handled via scheduled automatic retries rather than manual intervention each time. Full diff reviewed line-by-line before landing (no local gate re-run was possible, no local install in this worktree — CI's fresh install, including the real-Postgres integration job, was the verification). Two mechanical issues fixed post-review: the migration number collided with F09's already-merged `0054` (renumbered to `0056` — the dispatch worktree's base predated that merge) and two new `TRPCError` messages had been written in Arabic, inconsistent with every other error message in this file; changed to English to match.

## F07 — Shopify available quantity overwrites undifferentiated stock

**Confirmed live at baseline.** `inventory-levels-update`'s handler blindly overwrote `inventoryItems.quantity` with whatever Shopify reported — no check of which Shopify location the event came from, no check of whether the event was even newer than what was already applied, and no respect for IRTH being the authoritative source for physical stock. This needed a business decision before any fix could start (the plan's own dependency note flagged it): who wins when IRTH and Shopify disagree, and whether to build full multi-warehouse↔Shopify-location mapping now or bound the fix to the single already-selected location. Owner decided: **IRTH is authoritative** (matches the Phase 0 answer already given for the financial ledger), and scope is **bounded to the single already-chosen Shopify location** — full multi-warehouse mapping is separate, future work (see `[[2026-09 - IRTH OS Inventory Ownership (F07)]]` in the vault for the full record of this decision).

**Fix**: the event's `location_id` must match the connection's own `inventory_location_id` (converted from the webhook's numeric id via the existing `shopifyGid` helper) — a different or unselected location is skipped, not treated as a discrepancy. A new `inventoryItems.lastShopifyInventoryEventAt` column, independent of `updatedAt` (which also changes for unrelated IRTH-originated stock movements), tracks the last applied Shopify event's own timestamp — a not-strictly-newer event is skipped without effect. Once both checks pass, a mismatch between Shopify's reported quantity and IRTH's own is recorded in a new `inventory_level_discrepancies` table for review — `inventoryItems.quantity` is never written by this handler again. A matching report still advances the freshness marker so a later out-of-order duplicate is correctly recognized as stale.

**Verified against Shopify's own `inventory_levels/update` webhook reference**: numeric `location_id`, ISO 8601 `updated_at` with UTC offset — cited explicitly in the test file's own comments.

**Files changed**: `apps/api/src/routes/webhooks/shopify.ts`, `packages/db/src/schema/inventory.ts`, `packages/db/src/schema/inventoryDiscrepancies.ts` (new `inventoryLevelDiscrepancies` table, additive only — the existing order-shaped `inventoryDiscrepancies` table is untouched), new migration `0057_inventory_level_discrepancies.sql`, new test file.

**Explicitly out of scope**: multi-warehouse↔Shopify-location mapping (real, separate future work), a UI for reviewing/resolving recorded discrepancies.

**Process note**: implemented via `codex-delegate`. The dispatch that produced this fix hit the account's usage limit mid-turn before Codex could self-report, so this PR is the product of an independent line-by-line diff review rather than a verified self-report — found correct and complete as delivered, no delta needed. The new table follows this repo's own RLS convention without being asked to. No local gate re-run was possible (no local install in this worktree) — CI's fresh install, including the new test file's 11 individually-verified tests, was the real verification.

## F13 — purchase receipt completion checks only submitted lines

**Confirmed live at baseline.** `purchasing.receive`'s `fullyReceived` flag started `true` and was only ever set `false` by inspecting lines present in the call's own `items` array — a PO line never mentioned in a given submission could never flip it. Two live consequences: receiving just one line of a multi-line PO (fully filling only that line) marked the **whole PO** `'received'`; an **empty** `items` array left the flag at its initial `true`, marking the PO `'received'` with zero actual receiving.

**Fix**: after the per-item loop, completion is now computed from a fresh query over every line belonging to the PO (scoped to `poId` + `orgId`) — `'received'` only when all of them have `receivedQuantity >= quantity`. A submitted line that doesn't match this PO/org is now collected into a new `invalidItemIds` array in the response instead of a silent `continue`, while other valid lines in the same call still process normally.

**Files changed**: `apps/admin/src/server/routers/purchasing.ts`, `apps/admin/src/__tests__/routers/purchasing.test.ts` (6 new tests).

**Explicitly out of scope**: purchase-order creation/approval flow, weighted-average costing logic (both untouched).

**Process note**: implemented via `codex-delegate`, first real attempt after the account's usage limit reset. The dispatch worktree happened to have a working local install this time, so the orchestrator independently re-ran gates directly (`pnpm turbo lint typecheck test` — 12/12 tasks, all 17 purchasing tests pass, zero regressions across 298 admin + 120 api tests) rather than relying only on the implementer's self-report or CI. Full diff reviewed line-by-line — clean, minimal, matched the brief exactly, no delta needed.

## F19 — sensitive org settings stored plaintext, no key allowlist, non-atomic batch writes

**Confirmed live at baseline.** `settings.set`/`setMany` accepted any string as `key` (a typo silently created a new, uncatalogued row); the 5 `SENSITIVE_KEYS` values (WhatsApp/Resend API keys, ETA client secret, courier webhook secrets) were stored as **plain readable text** in `org_settings.value` — masking only ever happened in the API response and the audit log, never in storage, so a direct database read exposed them in full; and `setMany` wrote each key in its own separate transaction, so a failure partway through left a partial batch with only one audit entry covering whatever happened to succeed.

**Fix**: `SETTING_KEYS`'s existing catalog is now flattened into a zod enum and enforced on both mutations (`value` capped at 2000 chars). Sensitive values are AES-256-GCM encrypted (`node:crypto` — apps/admin runs on Node, not Workers) before being written, stored as `enc:v1:<IV>:<tag>:<ciphertext>` in the existing text column; each value's AAD binds it to its `(orgId, key)` pair so a ciphertext copied to a different org or setting key fails to decrypt rather than silently succeeding (a genuine addition beyond what was asked). `getAll` still only ever returns the mask for sensitive keys — a storage-layer fix, not an API change; a legacy plaintext value still reads and masks correctly, and only becomes encrypted once re-saved. `setMany` now performs every write plus the single audit entry inside one transaction.

**Files changed**: `apps/admin/src/server/routers/settings.ts`, `apps/admin/src/lib/settings.ts`, `apps/admin/src/app/[locale]/(dashboard)/settings/actions.ts`, `apps/admin/src/__tests__/routers/settings.test.ts` (36 tests), `apps/admin/src/__tests__/integration/settings.test.ts` (new, 4 tests). One existing RBAC test used an invalid placeholder key (`"a"`) that the new allowlist now correctly rejects — updated to a real key (`org.name`).

**Explicitly out of scope**: a key-rotation UI, a secrets-manager integration, backfilling existing plaintext values (each needs to be re-entered and saved once the key is live to actually get encrypted).

**Process note**: implemented via `codex-delegate` (`gpt-6-astra`, high reasoning effort — reserved for security-sensitive work per standing model-tiering guidance; routine dispatches use `gpt-5.6-terra`/`gpt-5.6-luna`). First attempt hit an internal Codex tooling bug — its own `apply_patch` verification rejected a malformed multi-operation patch targeting `settings.ts` — and crashed mid-turn with 5 files already touched. Rather than discard that progress or land it blind, the same Codex thread was resumed (`--session`) and asked to re-verify its own prior edit against disk before doing anything else; that resume additionally hit the account's usage limit and was resumed again once it reset. The second resume confirmed the file was complete and consistent, fixed one incidental fallout (the RBAC test's invalid key), and self-reported gates passing. The orchestrator independently re-ran gates in the dispatch worktree (326/326 admin tests, lint clean) and additionally confirmed the 4 real-Postgres integration tests directly in the CI log after merge — closing the one gap the local re-run couldn't reach (no DB access in that pass). Full diff reviewed line-by-line before landing.

## F10 — returns.create had no idempotency-key protection

**Confirmed live at baseline.** `returns.create` had no idempotency-key protection, unlike `customers.addPoints`, `giftCards.topup`/`redeem`, and `purchasing.receive` — all already using this repo's `withIdempotency` mechanism. A retried create-return request (timeout, double-tap) would create a second, entirely separate return record for the same actual return — not caught by the atomic claims already in `restock`/`updateStatus`, which guard concurrent processing of one *existing* row, not a retried *create* producing two.

**Fix**: `idempotencyKey: z.string().min(1).max(255).optional()` added to `create`'s input, wrapped in `ctx.idempotent('returns.create', input.idempotencyKey, input, ...)` — matching `purchasing.receive`'s exact shape. Opt-in, per the mechanism's own design.

**Files changed**: `apps/admin/src/server/routers/returns.ts`, `apps/admin/src/__tests__/routers/returns.test.ts`.

**Explicitly out of scope**: `restock`/`updateStatus` (already covered by their own atomic claims), any other router.

**Process note**: implemented via Google Jules. One real issue caught and fixed on review before landing: the idempotencyKey field's comment was copy-pasted verbatim from `purchasing.receive`'s own comment ("receiving twice adds the quantity to stock twice") — accurate there, wrong here, since this operation's actual risk is a duplicated *record*, not a doubled quantity. Corrected before committing.

## F11 — outbox worker has no atomic claim, overlapping cron runs can double-process

**Confirmed live at baseline.** `processOutbox` selected pending events with a plain SELECT — no row lock, no claim write before the provider-call loop. It runs on a Cloudflare Workers cron trigger, which can overlap under load or during a deploy; two overlapping invocations selecting the same event both proceeded to call the same provider (the same WhatsApp/email message sent twice, the same product pushed to Shopify twice, the same ETA invoice submitted twice) before either write landed.

**Fix**: atomic claim in its own transaction, before any provider call. A new nullable `claimedAt` column tracks claim state; the claim query selects rows where `claimed_at IS NULL OR claimed_at < now() - 5min` (a stale claim from a crashed worker is reclaimable) with `SELECT ... FOR UPDATE SKIP LOCKED`, then marks exactly those rows claimed — all inside one transaction, before provider calls (which stay outside the transaction, matching this repo's convention). Per-event-type handling, attempts/lastError bookkeeping, and the attempts<5 ceiling are unchanged.

**Files changed**: `apps/api/src/workers/outboxWorker.ts`, `packages/db/src/schema/outbox.ts`, new migration `0058_outbox_claim.sql`, new `outboxWorker.claim.test.ts`, plus mechanical mock updates (transaction wrapper, `.for()` chainable) to 3 existing outbox test files — no behavior change to what they test.

**Explicitly out of scope**: none — the fix is fully scoped to the claim mechanism.

**Process note**: implemented via Google Jules. One real issue caught and fixed on review before landing: Jules generated its migration as `0057_outbox_claim.sql` against an earlier `main` — F07 has since claimed `0057` for its own migration — renumbered to `0058` (the actual next-available slot) before staging.

**Process note on Jules status tracking**: `jules remote list --session`'s status column showed blank (never "Completed") for both F10 and F11 across their entire ~9h/~11h runtime, despite both sessions having genuinely finished work ready to pull hours earlier. `jules remote pull --session <id>` succeeded immediately and returned complete, real diffs the moment it was actually tried — the list command's status display should not be trusted as a completion signal; poll by pulling directly instead.

## F12 — outbox worker hammers external providers on every cron tick during an outage

**Scope note**: the original review's shorthand for this finding ("adapter parity") had no surviving detail anywhere — not in this file, not in the repo, not preserved from the original 20-finding review. Rather than dispatch a guessed brief, the orchestrator independently re-grounded this against the current code before writing one.

**Confirmed live at baseline.** `apps/api/wrangler.toml`'s cron trigger fires `processOutbox` every minute (`crons = ["* * * * *"]`). The `eta.invoice.issue` branch has its own deliberate exponential backoff; every other event type (`shopify.product.push`, `org.invite.sent`, `order.confirmed`, `order.shipped`) had none — a failure just let the row be claimed again on the very next tick. During a transient outage of WhatsApp, the transactional email provider, or Shopify, this hammered the failing provider once a minute and burned through the whole `attempts<5` dead-letter budget in 5 minutes flat — a notification that would have gone through fine with a few minutes of real spacing was instead permanently dead-lettered.

**Fix**: new `nextRetryAt` column on `outboxEvents` (independent of ETA's own `etaInvoices.nextRetryAt`, left untouched), set in the catch block for every event type except ETA using ETA's exact same `min(2^attempts, 60)`-minute formula, for consistency rather than a different curve. The claim query (inside F11's claim transaction) now also excludes rows whose `nextRetryAt` hasn't arrived, so a cooling-down row is never claimed at all — not claimed then skipped, wasting a slot from `OUTBOX_BATCH_SIZE` for nothing.

**Files changed**: `apps/api/src/workers/outboxWorker.ts`, `packages/db/src/schema/outbox.ts`, new migration `0059_outbox_retry_backoff.sql`, new `outboxWorker.retryBackoff.test.ts`, plus one line in the pre-existing `shopifyOutboxLegacyFallback.test.ts` (see process note).

**Explicitly out of scope**: ETA's own backoff mechanism, the `attempts<5` dead-letter ceiling, per-event-type provider logic — none touched.

**Process note**: implemented via `codex-delegate` (`gpt-5.6-terra` — routine complexity per this session's model-tiering guidance; `gpt-6-astra` reserved for security-sensitive work). No local install was available in the dispatch worktree (an install attempt stalled) — gates were never run before landing, unlike every other fix this session. CI caught a real gap on the first push: `shopifyOutboxLegacyFallback.test.ts`'s pre-existing exact-match assertion on a `shopify.product.push` failure path didn't anticipate the new `nextRetryAt` field now correctly present on that same call — the underlying behavior was correct, the test's shape assumption was stale. Fixed (`expect.any(Date)` added to the assertion) and re-pushed; CI passed clean on the second run.

## Sentinel — weak RNG for organization invite OTPs

Not part of the reviewed 20-finding plan — surfaced independently by `.jules/sentinel.md`'s own autonomous recurring security-scan routine on this repo (unrelated to this session's dispatches), and reviewed here before merging per the same "only Claude Code merges after review" standing practice applied to every other change.

**Confirmed live at baseline.** `packages/db/src/invites.ts`'s `generateInviteOtp` used `Math.random()` for the 6-digit invite OTP — not cryptographically secure, in principle predictable if the PRNG's internal state were known.

**Fix**: `crypto.getRandomValues(new Uint32Array(1))[0] % 900000` instead. Verified on review: the modulo bias from a non-power-of-2 divisor against a uint32 range is negligible here (2^32 ÷ 900000 ≈ 4772×, far below any practical threat for a rate-limited 6-digit OTP) — not worth rejection-sampling for perfect uniformity. `crypto.getRandomValues` (Web Crypto, not `node:crypto`) is the correct choice for this specific file since `packages/db` is shared by both the Cloudflare Workers API and the Node admin app, and only the Web Crypto API works natively in both.

**Files changed**: `packages/db/src/invites.ts`, `.jules/sentinel.md` (the routine's own learning-journal entry).

**Process note**: no dedicated regression test was added for this one-line RNG swap; existing CI coverage for `generateInviteOtp`'s contract (still returns a 6-digit numeric string) passed unchanged.

